name: CI

# Product-line CI for the ImL1s fork.
# `main` stays a pristine upstream mirror (no fork workflows there).
# Feature / sync PRs target `providers`; pushes to `providers` also run CI.

on:
  push:
    branches: [providers]
  pull_request:
    branches: [providers]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"

jobs:
  audit-publisher:
    name: Audit publisher tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Validate checked-in audit manifest
        run: python3 -B scripts/publish-audit-issues.py --validate-only

      - name: Run publisher unit tests
        run: python3 -B -m unittest discover -s tests -p 'test_audit_issue_publishers.py' -v

      - name: Enforce credential-safe observability
        run: python3 -B -m unittest tests.test_credential_observability_guard -v

      # The `new-test-filters` job below is the thing that notices a Rust test
      # no filter selects. These are its own tests -- without them it would be
      # an unverified checker guarding against unverified tests (#171).
      - name: Test the new-test filter guard
        run: python3 -B -m unittest tests.test_new_test_filter_guard -v

  installer:
    name: Installer
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # The installer is the one thing here that runs on machines unlike this
      # one, and until now nothing exercised it at all — every claim about it
      # rested on one macOS laptop. See #79.
      - name: Lint for POSIX sh
        run: |
          shellcheck -s sh install.sh
          dash -n install.sh
          dash -n tests/installer/run_cases.sh

      - name: Behaviour, with curl
        run: sh tests/installer/run_cases.sh

      # `select_downloader` prefers curl whenever it is present, so on any
      # normal machine the wget branch never runs. That is how it stayed
      # unexercised. This run hides curl.
      - name: Behaviour, with wget only
        env:
          EXCLUDE_DOWNLOADER: curl
        run: sh tests/installer/run_cases.sh

      # The fixture rewrites the GitHub hosts, so it cannot catch a mistake in
      # the real URLs — percent-encoding of `+` in a tag, say. One live install
      # covers exactly that, and nothing else does.
      - name: Install the published release for real
        run: |
          set -eux
          home="$(mktemp -d)"
          HOME="$home" sh install.sh
          HOME="$home" "$home/.medley/bin/medley" --version
          test ! -e "$home/.grok"

  sync-upstream:
    name: Sync upstream script
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Lint sync-upstream shell scripts
        run: |
          shellcheck -s bash scripts/sync-upstream.sh
          bash -n scripts/sync-upstream.sh
          shellcheck -s sh tests/sync-upstream/run_cases.sh
          dash -n tests/sync-upstream/run_cases.sh

      - name: Behaviour, local bare remotes
        run: sh tests/sync-upstream/run_cases.sh

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93.0"
          components: rustfmt

      - name: cargo fmt --check
        run: cargo fmt --all -- --check

  new-test-filters:
    name: New tests run somewhere
    runs-on: ubuntu-latest
    steps:
      # Full history: the check needs a merge base to find what this branch added.
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      # No Rust toolchain and no build: this reads the diff, not a test listing.
      # That is what makes it cheap, and it is also what makes it correct for
      # `#[cfg(target_os = ...)]` tests -- they are in the diff on every
      # platform, whereas `cargo test -- --list` omits them off their target.
      - name: Every new test matches a CI filter
        run: |
          python3 scripts/check_new_tests_are_filtered.py \
            --workflow .github/workflows/ci.yml \
            --base "origin/${{ github.base_ref || 'providers' }}"

  lint:
    name: Clippy (hot path)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93.0"
          components: clippy

      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: providers-lint
          cache-targets: true

      - name: Clippy sampler / shell / pager / pager-bin (all targets)
        env:
          PROTOC: protoc
        run: |
          # Measured 2026-08-07 on providers (#194): `--all-targets` on these
          # four crates is clean after fixing the handful of test/bench lints
          # (see the PR). No `-A` allow-list — that would re-open the blind
          # spot this job is meant to close. Full-workspace clippy remains
          # too slow; we stay on the hot-path crates only.
          #
          # `xai-grok-pager-bin` is a binary crate: `--lib` never saw it, and
          # that is how `.err().expect()` lived green in CI since 5c8144e.
          cargo clippy --manifest-path crates/codegen/xai-grok-sampler/Cargo.toml --all-targets -- -D warnings
          cargo clippy --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --all-targets -- -D warnings
          cargo clippy --manifest-path crates/codegen/xai-grok-pager/Cargo.toml --all-targets -- -D warnings
          cargo clippy --manifest-path crates/codegen/xai-grok-pager-bin/Cargo.toml --all-targets -- -D warnings

  test:
    name: Tests (providers hot path)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93.0"

      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: providers-test
          cache-targets: true

      - name: Sampler auth / wire tests
        env:
          PROTOC: protoc
        run: |
          cargo test --manifest-path crates/codegen/xai-grok-sampler/Cargo.toml \
            --test shared_http_wire none_auth_scheme_ -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-sampler/Cargo.toml \
            none_scheme_ -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-sampler/Cargo.toml \
            hostile_injector -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-sampler/Cargo.toml \
            omits_xai_identity -- --nocapture

      - name: Credential relation and telemetry export guards
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            grep -Eq 'test result: ok\. [1-9][0-9]* passed;' "$output" || {
              echo "test filter matched zero tests: $*" >&2
              rm -f "$output"
              return 1
            }
            rm -f "$output"
          }
          run_nonzero -p xai-grok-auth --features middleware --lib credential -- --nocapture
          run_nonzero -p xai-grok-telemetry --lib unified_log -- --nocapture
          # #121: for_test is the single place that names SamplingConfig fork
          # fields. Unique substrings — run_nonzero only fails on *zero* matches.
          run_nonzero -p xai-grok-sampling-types --lib \
            sampling_config_for_test_sets_fork_endpoint_trust_to_none -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            sampling_config_for_test_fru_inherits_fork_endpoint_trust_without_naming_it -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            final_relation_observes_hostile_injector_replacement_and_removal -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            post_capture_is_immune_to_resolver_rotation_after_build -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            sampler_request_logs_never_emit_credential_bytes -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            transport_failure_never_emits_query_credential_bytes -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            provider_controlled_stream_error_is_secret_free_for_all_stream_apis -- --nocapture
          run_nonzero -p xai-grok-mcp --lib \
            callback_errors_never_echo_provider_controlled_credentials -- --nocapture
          # #151: an unlabelled chat-state credential reaching an external origin
          # on `Unknown(CatalogUnavailable)`. Closed in code by #136, and the two
          # tests that pin it matched no filter at all -- so the highest-stakes
          # property in this fork was defended by tests no job ran (#171).
          #
          # Deliberately in this step rather than the auth one above: that step
          # uses bare `cargo test`, where a filter matching zero tests exits 0.
          # For a security property "the filter stopped matching" and "the
          # property still holds" must not look the same, and only `run_nonzero`
          # tells them apart.
          run_nonzero -p xai-grok-shell --lib catalog_unavailable -- --nocapture
          # #132: the reader's dup must not be inherited by children. Named
          # explicitly rather than by a `pty_session` prefix: the rest of that
          # module is the hang this change fixes, and this one is the fd leak
          # the fix introduced.
          run_nonzero -p xai-grok-shell --lib \
            dup_fd_is_not_inherited_by_exec_child -- --nocapture
          # #13: env-key normalization. In this step rather than the auth one
          # above because one of these asserts diagnostics carry the normalized
          # *name* and never a value -- and that step uses bare `cargo test`,
          # where a filter matching zero tests exits 0. `env_keys_` selects all
          # seven new tests and their three existing siblings.
          run_nonzero -p xai-grok-shell --lib env_keys_ -- --nocapture
          run_nonzero -p xai-grok-mcp --lib \
            callback_payload_debug_never_emits_code_state_or_issuer -- --nocapture
          run_nonzero -p xai-file-utils --lib \
            final_401_relation_uses_final_wire_credential_and_post_response_snapshot -- --nocapture
          run_nonzero -p xai-file-utils --lib \
            upload_config_debug_redacts_all_secret_carriers -- --nocapture

      - name: Provider docs examples and fixtures
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            grep -Eq 'test result: ok\. [1-9][0-9]* passed;' "$output" || {
              echo "test filter matched zero tests: $*" >&2
              rm -f "$output"
              return 1
            }
            rm -f "$output"
          }
          # #31: validate executable provider docs. Unique filters so
          # run_nonzero proves each docs guard still runs after refactors.
          run_nonzero -p xai-grok-shell --lib \
            docs_examples_extractor_handles_markers_ignores_unmarked_and_rejects_malformed -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            docs_examples_parse_and_resolve_provider_toml_examples -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            docs_examples_enforce_readiness_and_secret_policy -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            docs_examples_validate_offline_commands_and_links -- --nocapture

      - name: Update channel isolation guards
        # The fork's worst failure mode is a medley binary self-updating into
        # upstream's release channel and replacing itself with the official CLI
        # (issue #71). The release workflow trusts this job, and it only checks
        # that the distribution marker is present in the shipped binary — these
        # are what prove the marker actually stops an update.
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            grep -Eq 'test result: ok\. [1-9][0-9]* passed;' "$output" || {
              echo "test filter matched zero tests: $*" >&2
              rm -f "$output"
              return 1
            }
            rm -f "$output"
          }
          run_nonzero -p xai-grok-update --lib dist_channel -- --nocapture
          run_nonzero -p xai-grok-update --lib self_update -- --nocapture
          run_nonzero -p xai-grok-update --test test_dist_channel_gate -- --nocapture

      - name: Leader lock identity (Linux-only /proc probe)
        # The leader auto-kill path proves "is this OUR process" by checking
        # that the PID holds an open descriptor on the lock file (issue #158).
        # The safety half — never evict a process we cannot prove is ours — is
        # a pure unit test that runs everywhere. The POSITIVE half needs
        # /proc/<pid>/fd, so it is #[cfg(target_os = "linux")] and cannot run
        # on the macOS machines this project is developed on. Without this
        # step it would run nowhere at all, and "eviction can now fire" would
        # ship with zero executions of the test proving it can.
        #
        # run_nonzero fails on a zero-match filter, so this also catches the
        # test later being renamed, moved, or cfg'd out — the exact way it
        # silently stopped being covered before.
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            grep -Eq 'test result: ok\. [1-9][0-9]* passed;' "$output" || {
              echo "test filter matched zero tests: $*" >&2
              rm -f "$output"
              return 1
            }
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib holder_has_lock_file_open -- --nocapture

      - name: Restricted subagent capability boundaries
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            grep -Eq 'test result: ok\. [1-9][0-9]* passed;' "$output" || {
              echo "test filter matched zero tests: $*" >&2
              rm -f "$output"
              return 1
            }
            rm -f "$output"
          }
          run_nonzero -p xai-grok-tools --lib \
            restricted_capability_matrix_finalizes_and_dispatches_with_one_policy -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            readonly_gateway_target_rejects_unclassified_and_external_mutation -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            issue39_deploy_app_requires_all_capability_mode -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            restricted_gateway_snapshot_exposes_only_trusted_permitted_tools_per_session -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            restricted_children_reject_agent_owned_mcp_servers_before_session_spawn -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_cleanup_rejected_spawn_worktree_removes_only_fresh_worktrees -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_harness_rejection_cleans_fresh_worktree_before_handoff -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_pre_handoff_guard_cleans_worktree_on_panic -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_verbatim_fork_tools_intersect_session_admission -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_rebuild_respects_capability_mode_ceiling -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            explicit_gateway_cache_invalidation_waits_for_all_session_admissions -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_gateway_invalidation_includes_registered_child_sessions -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_aborted_explicit_gateway_refresh_recovers_waiters -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            gateway_refresh_fence_prevents_stale_snapshot_reconciliation -- --nocapture
          run_nonzero -p xai-grok-shell-session-support --lib \
            explicit_gateway_refresh_fence_clears_only_on_fresh_commit -- --nocapture
          run_nonzero -p xai-grok-shell-session-support --lib \
            legacy_config_invalidation_preserves_gateway_catalog -- --nocapture
          run_nonzero -p xai-grok-shell-session-support --lib \
            issue39_abort_gateway_tool_fetch_recovers_waiters -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            subagent_trusted_tool_capabilities_render_in_human_and_json_views -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            resolve_session_toolset_mcp_kind_none_dropped_under_readonly -- --nocapture
          run_nonzero -p xai-computer-hub-sdk --lib \
            unavailable_handlers_are_absent_from_publish_and_dispatch -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            dispatch_configure_mcp -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            qualified_handler_fails_closed_when_session_generation_expires -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            unrestricted_generation_guard_rejects_reused_session_id -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            drop_cleanup_clears_old_generation_resources_written_in_flight -- --nocapture

      - name: Agent serve exposure and authentication
        env:
          PROTOC: protoc
        run: |
          set -o pipefail
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            grep -Eq 'test result: ok\. [1-9][0-9]* passed;' "$output" || {
              echo "test filter matched zero tests: $*" >&2
              rm -f "$output"
              return 1
            }
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            issue8_agent_serve_security_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_gateway_queue_rejects_when_full -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_oversized_trailing_ping_closes_with_1009 -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_oversized_empty_after_trim_closes_with_1009 -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_oversized_non_utf8_binary_closes_with_1009 -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue8_agent_serve_security_ -- --nocapture
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            issue8_agent_serve_security_ -- --nocapture

      - name: "Leader kill safety (issue #167)"
        # `leaders kill` used to ask "is this process named grok?", which is
        # false for the shipped `medley` binary — so it deleted a LIVE leader's
        # lock and socket without signalling it, and the next client acquired
        # the flock on a fresh inode: two live leaders from a command whose
        # purpose is to leave none.
        #
        # These pin the two invariants that replaced it: a socket-verified live
        # leader is signalled and its files are never touched, and a lock file
        # is reclaimed only when no live process holds its flock. Neither
        # consults a process name, so neither can rot on the next rename.
        #
        # Needed because ci.yml has no unfiltered job (#171): the only other
        # xai-grok-pager-bin invocation filters on `issue8_agent_serve_security_`,
        # so without this step these tests would run nowhere. `run_nonzero`
        # fails on a zero-match filter, so renaming them breaks CI loudly
        # instead of silently dropping the coverage.
        env:
          PROTOC: protoc
        run: |
          set -o pipefail
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            grep -Eq 'test result: ok\. [1-9][0-9]* passed;' "$output" || {
              echo "test filter matched zero tests: $*" >&2
              rm -f "$output"
              return 1
            }
            rm -f "$output"
          }
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            leaders_kill -- --nocapture
          # Unique selector for #183: proves the race-repro test itself runs
          # (shared `leaders_kill` prefixes would stay green if it were deleted).
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            leaders_kill_issue183_interleaving_preserves_new_leader_socket -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            leader::lock::tests::reclaim -- --nocapture

      - name: Shell auth + readiness meta
        env:
          PROTOC: protoc
        run: |
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            none_auth_scheme -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            acp_model_meta_ -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            agent::auth_method -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            config_model_override_parse -- --nocapture
          # #13: env_key trim/validate/dedupe + path-specific invalid-name warnings.
          # Unique prefixes so run_nonzero proves the new tests are selected.
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero --lib env_keys_normalize_ -- --nocapture
          run_nonzero --lib env_keys_diagnostics_show_normalized_names_only -- --nocapture
          run_nonzero --lib env_keys_invalid_entry_emits_path_specific_config_warning -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            read_parent_sampling_config_ -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            handle_set_session_model_clears_credentials_for_none -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            catalog_key -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            auth_scheme -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            sampling_config_for_unready -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            credential_header -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            invalid_auth_scheme -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            relation_matrix_is_secret_free -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            callback_errors_never_echo_provider_controlled_values -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            snapshot_never_derives_stable_ids_from_credentials -- --nocapture
          # #132: CLOEXEC regression guard; full test path keeps the filter
          # unique so #171 can prove this exact test is selected.
          cargo test --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --lib \
            terminal::pty_session::tests::dup_fd_is_not_inherited_by_exec_child -- --nocapture

      - name: "Substituted default model meta (#131)"
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            grep -Eq 'test result: ok\. [1-9][0-9]* passed;' "$output" || {
              echo "test filter matched zero tests: $*" >&2
              rm -f "$output"
              return 1
            }
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            absent_configured_default_is_substituted_and_reported -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            present_but_not_selectable_configured_default_is_substituted_and_reported -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            honoured_configured_default_reports_no_substitution -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            campaign_driven_default_is_not_reported_as_the_users_choice -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            configured_unready_default_already_publishes_its_reason_per_model -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            initialize_publishes_substituted_default_model_meta -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            initialize_omits_substituted_default_model_meta_when_honoured -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            reselect_if_missing_clears_stale_substitution_on_early_return -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            clear_wipes_substituted_preference -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            models_update_meta_clears_substitution_after_catalog_lands -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            warm_cache_refresh_does_not_reseat_over_user_model_pick -- --nocapture

      - name: Pager model picker / readiness
        env:
          PROTOC: protoc
        run: |
          cargo test --manifest-path crates/codegen/xai-grok-pager/Cargo.toml --lib \
            slash::commands::model:: -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-pager/Cargo.toml --lib \
            hard_blocks_ -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-pager/Cargo.toml --lib \
            catalog_miss -- --nocapture
          cargo test --manifest-path crates/codegen/xai-grok-pager/Cargo.toml --lib \
            toctou -- --nocapture
          # The three tests this change fixes matched none of the filters
          # above, so registering their models bought nothing: they ran in no
          # job at all (#171). This selects them and the six sibling
          # deferred-switch tests, which belong to this step by subject and
          # were equally unrun.
          cargo test --manifest-path crates/codegen/xai-grok-pager/Cargo.toml --lib \
            deferred_switch_ -- --nocapture

      # #159: a runtime-only model was judged absent by a config-only lookup,
      # and the turn stripped its credentials. Filters written out rather than
      # looped, so the #171 check can see them. Quoted name: an unquoted `#159`
      # truncates at the comment marker.
      - name: 'Runtime-catalog lookup and memo generation (issue #159)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib runtime_catalog -- --nocapture
          run_nonzero -p xai-grok-shell --lib runtime_only_catalog -- --nocapture
          run_nonzero -p xai-grok-shell --lib config_only_miss -- --nocapture
          run_nonzero -p xai-grok-shell --lib memoized_as_definite -- --nocapture
          run_nonzero -p xai-grok-shell --lib catalog_refresh_that_drops -- --nocapture
          run_nonzero -p xai-grok-shell --lib bundled_fallback_rebuild -- --nocapture
          # `agent::models` as a module filter: the generation asserts added to
          # the pre-existing `rebuild_updates_models_and_available` live there
          # and matched no name-based filter, so that guard ran nowhere. A new
          # assertion in an old unfiltered test is invisible to the #171 check,
          # which looks for added `#[test]` attributes.
          run_nonzero -p xai-grok-shell --lib agent::models -- --nocapture

      # #166: these assert on colour, and used to inherit the ambient colour
      # level. Run them under both settings of NO_COLOR -- one pass alone would
      # not have caught the original bug, because it only showed up on the axis
      # CI never varied. Quoted name: an unquoted `#166` truncates at the
      # comment marker.
      - name: 'Colour-asserting pager tests, both NO_COLOR settings (issue #166)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          # Filters written out rather than looped over a shell variable, so
          # they stay greppable in this file -- the #171 check parses these
          # lines, and a filter built at runtime is invisible to it.
          #
          # Do not use bare `teal`: Cargo substring-matches, and `steal` /
          # `steals` / `resteal` contain `teal`, as does
          # `teal_highlighting_on_second_line` (which skips its colour assert
          # under NO_COLOR). `keeps_teal` + `each_teal` select only the four
          # fixed skill-accent tests that filter on accent_skill spans.
          run_nonzero -p xai-grok-pager --lib keeps_teal -- --nocapture
          run_nonzero -p xai-grok-pager --lib each_teal -- --nocapture
          run_nonzero -p xai-grok-pager --lib invalid_token_ranges_are_dropped -- --nocapture
          run_nonzero -p xai-grok-pager --lib token_on_second_logical_line -- --nocapture
          run_nonzero -p xai-grok-pager --lib search_bar_cursor -- --nocapture
          # Made hermetic by this change like the others, but pre-existing, so
          # the #171 check cannot see it: that check reads the diff for a new
          # `#[test]`, and this test is not new -- only its contract is.
          run_nonzero -p xai-grok-pager --lib \
            user_prompt_band_is_semantic -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            colour_asserting_tests_do_not_use_theme_current -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib keeps_teal -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib each_teal -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib invalid_token_ranges_are_dropped -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib token_on_second_logical_line -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib search_bar_cursor -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib \
            user_prompt_band_is_semantic -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib \
            colour_asserting_tests_do_not_use_theme_current -- --nocapture

      # #162: the restore-direction and the tombstone-wall cases. The
      # pre-existing `workflow_restore_rejects_symlinks_and_caps_run_count`
      # stays green under both defects, so `workflow_restore` as one filter
      # covers all three and the two new ones carry the protection. Quoted
      # name: an unquoted `#162` truncates at the comment marker.
      - name: 'Workflow restore ordering and scan budget (issue #162)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib workflow_restore -- --nocapture

      # #165: abandoned sampling attempts must not enter the FTS index.
      # Unique filters so run_nonzero proves each new test is selected.
      # Quoted name: an unquoted `#165` truncates at the comment marker.
      - name: 'Abandoned attempt text omitted from search index (issue #165)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_discards_abandoned_retry_attempt_phrase -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_discards_attempt_discarded_retraction_phrase -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_still_flushes_on_rewind_marker_boundary -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_still_flushes_on_compaction_checkpoint_boundary -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_completed_turn_still_indexes_across_hook_annotation -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            single_pass_omits_abandoned_retry_attempt_from_fts_content -- --nocapture

      # #180: an ambient xAI session JWT under a post-strip `ExplicitHeader`
      # label reaching a third-party origin. Both halves of the fix are
      # independently mutation-proven, so both filters are needed -- reverting
      # either one reddens only its own test. Quoted name: an unquoted `#180`
      # truncates at the comment marker.
      - name: 'Ambient credential label coupling (issue #180)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-sampler --lib \
            ambient_token_labelled_explicit_header -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            dual_auth_gateway_model_key_plus_declared_header -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            seeded_ambient_key_must_not_keep_a_post_strip -- --nocapture
          # Seam half of #180: reconstruct / parent-inherit must not invent
          # ExplicitHeader from the maps while keeping api_key. The sampler
          # dual_auth_gateway_… test never reaches these functions.
          run_nonzero -p xai-grok-shell --lib \
            reconstruct_full_config_ready_dual_auth_keeps_model_api_key -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            read_parent_sampling_config_dual_auth_keeps_model_api_key -- --nocapture

      # #160: six of the seven tests this branch originally added matched no
      # filter. The seventh, `web_search_resolves_an_env_backed_credential_header`,
      # is already selected by the existing `credential_header` filter. Written
      # out rather than looped so the #171 check can see them. Quoted name: an
      # unquoted `#160` truncates at the comment marker.
      - name: 'web_search env header forwarding (issue #160)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib spawn_gates_web_search -- --nocapture
          run_nonzero -p xai-grok-shell --lib spawn_forwards_env_http_headers -- --nocapture
          run_nonzero -p xai-grok-shell --lib web_search_env_header_overrides -- --nocapture
          run_nonzero -p xai-grok-shell --lib env_backed_credential_header -- --nocapture
          # Also this PR's earlier commit, whose three tests matched nothing either.
          run_nonzero -p xai-grok-shell --lib keeps_web_search_and_is_told_nothing -- --nocapture
          run_nonzero -p xai-grok-shell --lib credential_less_route -- --nocapture
          run_nonzero -p xai-grok-shell --lib invents_no_bearer -- --nocapture
          # Review follow-up: resolve→SamplerConfig map copy, and the
          # bearer_resolver arm of has_usable_credential.
          run_nonzero -p xai-grok-shell --lib survives_into_sampler_config -- --nocapture
          run_nonzero -p xai-grok-shell --lib accepts_a_bearer_resolver_alone -- --nocapture
          # #57 follow-up: web_search must fall back to [models].default and keep
          # model-scoped credential provenance when xAI ambient auth is absent.
          run_nonzero -p xai-grok-shell --lib falls_back_to_models_default_and_keeps_model_scoped_credential -- --nocapture

      # #161: the web_search disable notice now rides the session response
      # `_meta` instead of a notification. Two filters because the tests split
      # Exclusive substrings for the #161 tests only. A shared prefix like
      # `web_search_disable` also matches preexisting #57 config/renderer tests,
      # so `run_nonzero` would stay green if the new tests were deleted (#171).
      - name: 'web_search disable notice (issue #161)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib session_meta_publishes_the_web_search_disable -- --nocapture
          run_nonzero -p xai-grok-shell --lib take_session_drops_the_web_search_notice -- --nocapture
          run_nonzero -p xai-grok-pager --lib web_search_disabled_meta -- --nocapture
          run_nonzero -p xai-grok-pager --lib no_web_search_notice -- --nocapture
          run_nonzero -p xai-grok-pager --lib parse_session_web_search_disabled -- --nocapture

      # #167: pure seams split out of `kill_leaders` so summary/exit and PID
      # guards are unit-testable. The earlier "Leader kill safety" step already
      # selects `leaders_kill` + `reclaim_*` invariants; this step covers the
      # three helpers plus the failed-signal counter contract. Quoted name: an
      # unquoted `#167` truncates the YAML value at the comment marker.
      - name: 'leaders kill summary and PID guard (issue #167)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            format_kill_leaders_summary -- --nocapture
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            leader_kill_action -- --nocapture
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            format_leader_held_guidance -- --nocapture
          # Unique filter: proves the failed-kill → left_alone contract runs
          # (substring shared with `leaders_kill` alone would not).
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            leaders_kill_failed_kill_counts -- --nocapture
          # Unique filter: proves startup kill PID plausibility bounds run.
          run_nonzero -p xai-grok-shell --lib \
            startup_kill_socket_pid_plausibility_bounds -- --nocapture
          # `reclaim_` rather than the NotFound test alone: this branch's two
          # mutation-proven invariant tests (`reclaim_removes_a_lock_no_process_holds`,
          # `reclaim_refuses_a_lock_a_live_process_holds`) are also selected here.
          run_nonzero -p xai-grok-shell --lib reclaim_ -- --nocapture

      # #173: the arm/note_ready ordering invariant from #45 is defended by a
      # comment spanning two crates. These are source-scanning guards: the
      # shell test asserts respawn_stdio inserts into owned_clients BEFORE
      # arming the watcher and never awaits between the arm and Ok(()); the
      # mcp test asserts arm_liveness_watcher itself never awaits after
      # spawn_transport_liveness. Full test names as filters so each selects
      # its test uniquely. Quoted name: an unquoted `#173` truncates at the
      # comment marker.
      - name: 'MCP respawn arm/insert ordering guards (issue #173)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            respawn_stdio_then_inserts_before_arming_and_has_no_post_arm_yields -- --nocapture
          run_nonzero -p xai-grok-mcp --lib \
            arm_liveness_watcher_then_has_no_scheduler_yield_after_spawning -- --nocapture

      # #36: restore fd bundling only with a public PI caller and contract.
      # The tests are split by build mode: override-path bundle and release
      # auto-download bundle. Filters are written explicitly so #171 can prove
      # each new test runs somewhere.
      - name: 'PI fd glob contract and bundling (issue #36)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }

          run_nonzero -p xai-grok-tools --features pi --lib \
            pi_glob_fd_contract_preserves_public_glob_semantics -- --nocapture
          run_nonzero -p xai-grok-tools --features pi --lib \
            pi_glob_fd_error_falls_back_to_ripgrep -- --nocapture

          GROK_TOOLS_BUNDLE_FD_PATH="$PWD/crates/codegen/xai-grok-tools/tests/fd-override-fixture.sh" \
            run_nonzero -p xai-grok-tools --features pi --lib \
              bundled_fd_override_artifact_extracts_exact_and_executes -- --nocapture

          run_nonzero --release -p xai-grok-tools --features pi --lib \
            bundled_fd_release_artifact_extracts_exact_and_executes -- --nocapture

      # #88: `xai-proto-build` is run by every build script in this workspace and
      # was in no test job at all -- so the code that decides whether protoc is
      # findable, and what gets emitted to cargo's stdout, was never exercised by
      # CI. That stdout is parsed one directive per line, so a value carrying a
      # newline injects further directives; a non-UTF-8 path is exactly the input
      # that reaches it. Quoted name: an unquoted `#88` truncates at the comment
      # marker.
      - name: 'Build-script protoc discovery and rerun-if-changed emission (issue #88)'
        env:
          PROTOC: protoc
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-proto-build --lib -- --nocapture
