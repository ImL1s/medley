name: CI

# Product-line CI for the ImL1s fork.
# `main` stays a pristine upstream mirror (no fork workflows there).
# Feature / sync PRs target `providers`; pushes to `providers` also run CI.
#
# Every GitHub Action is pinned to a full commit SHA (issue #30); the
# version tag in the trailing comment is documentation only.

on:
  push:
    branches: [providers]
  pull_request:
    branches: [providers]
  workflow_dispatch:

concurrency:
  # Push runs are release candidates keyed by commit SHA; if a later merge lands
  # while one is running, we still need the earlier SHA to reach a completed
  # verdict for release.yml's SHA-exact gate.
  group: ci-${{ github.event_name == 'push' && github.sha || github.ref }}
  # PRs and manual runs should still drop superseded attempts.
  cancel-in-progress: ${{ github.event_name != 'push' }}

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"
  # Keep every Rust lane on the repository-pinned protobuf toolchain. Ubuntu
  # ships protoc 21.12, which predates FieldOptions.debug_redact and cannot
  # compile the upstream debug-redaction fixtures.
  PROTOC: ${{ github.workspace }}/bin/protoc

jobs:
  audit-publisher:
    name: Audit publisher tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      - name: Validate checked-in audit manifest
        run: python3 -B scripts/publish-audit-issues.py --validate-only

      - name: Run publisher unit tests
        run: python3 -B -m unittest discover -s tests -p 'test_audit_issue_publishers.py' -v

      - name: Enforce credential-safe observability
        run: python3 -B -m unittest tests.test_credential_observability_guard -v

      # The `new-test-filters` job below is the thing that notices a Rust test
      # no filter selects. These are its own tests -- without them it would be
      # an unverified checker guarding against unverified tests (#171).
      - name: Test the new-test filter guard
        run: python3 -B -m unittest tests.test_new_test_filter_guard -v

      - name: Test the uncovered-crate ratchet
        run: python3 -B -m unittest tests.test_uncovered_crates_guard -v

      - name: Test the PR-head CI receipt guard
        # This guard is a pre-merge local/orchestrator check; it does not run
        # as part of CI itself. CI only executes its Python unit tests.
        run: python3 -B -m unittest tests.test_pr_head_ci_run_guard -v

      - name: Guard distChannel docs against the release stamp
        run: python3 -B -m unittest tests.test_dist_channel_docs_guard -v

      - name: Enforce SHA-pinned GitHub Actions
        run: python3 -B -m unittest tests.test_workflow_action_pins -v

      # The user guide is extracted to <grok_home>/docs/user-guide/ at startup
      # and docs/architecture/ is not, so a relative link out of the guide
      # directory is broken for installed readers (#385 shipped one).
      - name: Guard user-guide links against install extraction
        run: python3 -B -m unittest tests.test_user_guide_link_guard -v

      # Direct-mention scan: EnvGuard / std::env::{set_var,remove_var} in
      # xai-grok-shell unit tests must carry unkeyed #[serial] (#319).
      # Known stragglers are allowlisted; new hits and stale entries fail.
      - name: Test the EnvGuard serial guard
        run: python3 -B -m unittest tests.test_envguard_serial_guard -v

      - name: Enforce EnvGuard tests are unkeyed #[serial]
        run: python3 -B scripts/check_envguard_serial.py

  installer:
    name: Installer
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      # The installer is the one thing here that runs on machines unlike this
      # one, and until now nothing exercised it at all — every claim about it
      # rested on one macOS laptop. See #79.
      - name: Lint for POSIX sh
        run: |
          shellcheck -s sh install.sh
          shellcheck -s sh tests/installer/target_selection_cases.sh
          shellcheck -s sh tests/release/musl_smoke.sh
          dash -n install.sh
          dash -n tests/installer/run_cases.sh
          dash -n tests/installer/target_selection_cases.sh
          dash -n tests/release/musl_smoke.sh

      - name: Behaviour, with curl
        run: sh tests/installer/run_cases.sh

      # Which Linux archive to download is decided before any of the above
      # runs, from the host's glibc version (#82). The interesting hosts are
      # ones this runner is not — below the floor, or with no glibc at all —
      # so the probes are stubbed rather than the case going untested.
      - name: Target selection across libc flavours
        run: sh tests/installer/target_selection_cases.sh

      # The musl smoke harness runs inside a release container, where a
      # syntax error would surface only at tag time. Importing it here is the
      # cheapest thing that keeps it honest between releases.
      - name: The musl smoke harness imports
        run: python3 -B -m py_compile tests/release/musl_smoke_server.py

      # `select_downloader` prefers curl whenever it is present, so on any
      # normal machine the wget branch never runs. That is how it stayed
      # unexercised. This run hides curl.
      - name: Behaviour, with wget only
        env:
          EXCLUDE_DOWNLOADER: curl
        run: sh tests/installer/run_cases.sh

      # The fixture rewrites the GitHub hosts, so it cannot catch a mistake in
      # the real URLs — percent-encoding of `+` in a tag, say. One live install
      # covers exactly that, and nothing else does.
      #
      # Resolve the published tag with the job token. Anonymous
      # /releases/latest shares a 60 req/h/IP budget across runners (#256).
      # Pinning MEDLEY_VERSION still exercises download, checksums, extract,
      # launcher, --version, and the ~/.grok assertion. Latest-lookup itself
      # is covered by tests/installer/run_cases.sh `ok` / `no-release`.
      - name: Install the published release for real
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -eux
          home="$(mktemp -d)"
          MEDLEY_VERSION="$(gh release view --repo "$GITHUB_REPOSITORY" --json tagName -q .tagName)"
          HOME="$home" MEDLEY_VERSION="$MEDLEY_VERSION" sh install.sh
          HOME="$home" "$home/.medley/bin/medley" --version
          test ! -e "$home/.grok"

  sync-upstream:
    name: Sync upstream script
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      - name: Lint sync-upstream shell scripts
        run: |
          shellcheck -s bash scripts/sync-upstream.sh
          bash -n scripts/sync-upstream.sh
          shellcheck -s sh tests/sync-upstream/run_cases.sh
          dash -n tests/sync-upstream/run_cases.sh

      - name: Behaviour, local bare remotes
        run: sh tests/sync-upstream/run_cases.sh

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: "1.94.0"
          components: rustfmt

      - name: cargo fmt --check
        run: cargo fmt --all -- --check

  new-test-filters:
    name: New tests run somewhere
    runs-on: ubuntu-latest
    steps:
      # Full history: the check needs a merge base to find what this branch added.
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          fetch-depth: 0

      # No Rust toolchain and no build: this reads the diff, not a test listing.
      # That is what makes it cheap, and it is also what makes it correct for
      # `#[cfg(target_os = ...)]` tests -- they are in the diff on every
      # platform, whereas `cargo test -- --list` omits them off their target.
      - name: Every new test matches a CI filter
        run: |
          python3 scripts/check_new_tests_are_filtered.py \
            --workflow .github/workflows/ci.yml \
            --base "origin/${{ github.base_ref || 'providers' }}"

      # Crate-level complement of the diff-scoped guard above (issue #280).
      # A crate with src/ tests that no `-p` names runs nowhere forever;
      # #171 only sees newly added tests. No Rust toolchain: this reads
      # Cargo.toml + src/**/*.rs + ci.yml against a checked-in allowlist.
      - name: Every crate with tests is named or allowlisted
        run: |
          python3 scripts/check_uncovered_crates.py \
            --workflow .github/workflows/ci.yml \
            --allowlist tests/ci/uncovered-crates.allowlist

  windows-session-publication:
    name: Windows session publication
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: "1.94.0"

      - name: Install pinned protoc
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $archive = Join-Path $env:RUNNER_TEMP "protoc-29.3-win64.zip"
          $protocRoot = Join-Path $env:RUNNER_TEMP "protoc"
          $protoc = Join-Path $protocRoot "bin\protoc.exe"
          $url = "https://github.com/protocolbuffers/protobuf/releases/download/v29.3/protoc-29.3-win64.zip"
          Invoke-WebRequest -Uri $url -OutFile $archive
          $actual = (Get-FileHash -Algorithm SHA256 $archive).Hash.ToLowerInvariant()
          $expected = "57ea59e9f551ad8d71ffaa9b5cfbe0ca1f4e720972a1db7ec2d12ab44bff9383"
          if ($actual -ne $expected) {
            throw "protoc archive SHA-256 $actual did not match $expected"
          }
          Expand-Archive -Path $archive -DestinationPath $protocRoot
          "PROTOC=$protoc" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          & $protoc --version

      - name: Cache cargo
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: providers-windows-session-publication
          cache-targets: true

      - name: Native ACL and no-replace publication contracts
        shell: pwsh
        run: |
          function Run-Nonzero {
            param([string[]] $CargoArgs)
            $log = New-TemporaryFile
            & cargo test @CargoArgs 2>&1 | Tee-Object -FilePath $log
            if ($LASTEXITCODE -ne 0) {
              throw "cargo test failed: cargo test $($CargoArgs -join ' ')"
            }
            if ((Get-Content -Raw $log) -match 'test result: ok\. 0 passed') {
              throw "filter matched zero tests: cargo test $($CargoArgs -join ' ')"
            }
          }
          Run-Nonzero @('-p', 'xai-grok-shell-base', '--lib', 'ensure_owner_only_directory_', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell-base', '--lib', 'windows_retained_handle_survives_root_rename_and_replacement_by_identity', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell-base', '--lib', 'windows_child_creation_and_no_replace_are_handle_relative', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell-base', '--lib', 'windows_retained_tree_removal_does_not_follow_junctions', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell-base', '--lib', 'windows_open_root_and_child_reject_reparse_points', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell-base', '--lib', 'windows_owner_only_lock_file_', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell-base', '--lib', 'failed_retained_rename_returns_the_live_source_anchor', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell', '--lib', 'codex_windows_last_scope_clear_replaces_with_empty_store', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell', '--lib', 'fresh_claim_does_not_follow_staging_junctions', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell', '--lib', 'relative_auth_path_parent_sync_uses_current_directory', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell', '--lib', 'nested_relative_auth_parent_creation_is_durable_and_isolated', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell', '--lib', 'no_replace_', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell', '--lib', 'final_publication_moves_private_stage_and_rebinds_storage_path', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell', '--lib', 'final_publication_collision_is_not_committed', '--', '--nocapture')
          Run-Nonzero @('-p', 'xai-grok-shell', '--lib', 'stale_public_marker_is_preserved_', '--', '--nocapture')

  macos-session-publication:
    name: macOS session publication
    runs-on: macos-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: "1.94.0"

      - name: Install DotSlash and verify pinned protoc
        run: |
          cargo install dotslash --version 0.5.7 --locked
          /usr/bin/env dotslash --help >/dev/null
          "$PROTOC" --version

      - name: Cache cargo
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: providers-macos-session-publication
          cache-targets: true

      - name: Anchored publication contracts
        run: |
          set -o pipefail
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell-base --lib anchored_directory:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib no_replace_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib final_publication_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            symlinked_cwd_parent_without_touching_outside_tree -- --nocapture

  compile-tests:
    name: Compile every test target
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: "1.94.0"

      - name: Install DotSlash and verify pinned protoc
        run: |
          cargo install dotslash --version 0.5.7 --locked
          /usr/bin/env dotslash --help >/dev/null
          "$PROTOC" --version

      - name: Cache cargo
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: providers-compile-tests
          cache-targets: true

      # #254: thirteen pager integration targets (and any other crate's tests)
      # are never named by a hot-path filter. Compiling them without running
      # is what catches a renamed type after an upstream sync.
      - name: cargo test --workspace --no-run
        run: cargo test --workspace --no-run

  lint:
    name: Clippy (hot path)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: "1.94.0"
          components: clippy

      - name: Install DotSlash and verify pinned protoc
        run: |
          cargo install dotslash --version 0.5.7 --locked
          /usr/bin/env dotslash --help >/dev/null
          "$PROTOC" --version

      - name: Cache cargo
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: providers-lint
          cache-targets: true

      - name: Clippy sampler / shell / tools / pager / pager-bin (all targets)
        run: |
          # Measured 2026-08-09 on providers (#275): `--all-targets` on these
          # five crates is clean after fixing the handful of test/bench lints
          # (see the PR). No `-A` allow-list — that would re-open the blind
          # spot this job is meant to close. Full-workspace clippy remains
          # too slow; we stay on the hot-path crates only.
          #
          # `xai-grok-pager-bin` is a binary crate: `--lib` never saw it, and
          # that is how `.err().expect()` lived green in CI since 5c8144e.
          cargo clippy --manifest-path crates/codegen/xai-grok-sampler/Cargo.toml --all-targets -- -D warnings
          cargo clippy --manifest-path crates/codegen/xai-grok-shell/Cargo.toml --all-targets -- -D warnings
          # Include the opt-in PI backend so its test-only environment lock is linted too.
          cargo clippy --manifest-path crates/codegen/xai-grok-tools/Cargo.toml --all-targets --features pi -- -D warnings
          cargo clippy --manifest-path crates/codegen/xai-grok-pager/Cargo.toml --all-targets -- -D warnings
          cargo clippy --manifest-path crates/codegen/xai-grok-pager-bin/Cargo.toml --all-targets -- -D warnings

  test:
    name: Tests (providers hot path)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: "1.94.0"

      # `ripgrep` is a runtime dependency of the grep tool, not a convenience:
      # `rg_path()` falls back to a bare `rg` on PATH when the crate is built
      # without `bundle_rg`, and a failed spawn surfaces as exit code -1 with
      # empty output. This job never needed it until the upstream sync brought
      # the `implementations::` filter that runs those tests, at which point 44
      # of them failed here and passed on a developer machine — because
      # developer machines have `rg`.
      - name: Install DotSlash and ripgrep
        run: |
          sudo apt-get update && sudo apt-get install -y ripgrep
          cargo install dotslash --version 0.5.7 --locked
          /usr/bin/env dotslash --help >/dev/null
          "$PROTOC" --version

      - name: Cache cargo
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: providers-test
          cache-targets: true

      # #343: temp-store fixtures must bind to their requested path even when
      # the caller already has an ambient auth override. Run both fixture-heavy
      # modules with the normal libtest parallelism so process-wide env leakage
      # cannot hide behind a serial harness.
      - name: Codex auth fixtures ignore hostile ambient auth path
        run: |
          set -o pipefail
          umask 077
          hostile_auth_dir="$(mktemp -d "${TMPDIR:-/tmp}/medley-hostile-auth.XXXXXX")"
          hostile_auth="$hostile_auth_dir/auth.json"
          test_output=""
          cleanup() {
            if [ -n "$test_output" ]; then
              rm -f "$test_output" 2>/dev/null || true
            fi
            rm -f "$hostile_auth" 2>/dev/null || true
            rmdir "$hostile_auth_dir" 2>/dev/null || true
          }
          trap cleanup EXIT
          trap 'exit 129' HUP
          trap 'exit 130' INT
          trap 'exit 143' TERM
          chmod 700 "$hostile_auth_dir"
          printf '%s\n' '{}' >"$hostile_auth"
          chmod 600 "$hostile_auth"
          export GROK_AUTH_PATH="$hostile_auth"

          run_nonzero() {
            test_output="$(mktemp "$hostile_auth_dir/test-output.XXXXXX")"
            cargo test "$@" 2>&1 | tee "$test_output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then
              rm -f "$test_output"
              test_output=""
              return "$status"
            fi
            if grep -qE '^test result: ok\. 0 passed' "$test_output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$test_output"
              test_output=""
              return 1
            fi
            rm -f "$test_output"
            test_output=""
          }
          run_nonzero -p xai-grok-shell --lib \
            auth::openai_codex::tests:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            auth::manager::tests:: -- --nocapture

      # #361: parser and auth-store unit tests cannot prove that the shipped
      # composition root dispatches the selected provider to Codex logout.
      - name: Provider-scoped Codex logout dispatch
        run: |
          set -o pipefail
          output="$(mktemp)"
          cargo test --locked -p xai-grok-pager-bin \
            --test auth_logout_dispatch \
            openai_codex_logout_dispatch_removes_only_medleys_provider_scope_without_network \
            -- --exact --nocapture 2>&1 | tee "$output"
          status="${PIPESTATUS[0]}"
          if [ "$status" -ne 0 ]; then rm -f "$output"; exit "$status"; fi
          if ! grep -qE '^test result: ok\. 1 passed; 0 failed;' "$output"; then
            echo "::error::Codex logout dispatch must run exactly one passing test"
            rm -f "$output"; exit 1
          fi
          rm -f "$output"

      # #363: the PKCE/callback/exchange/storage pieces are individually tested,
      # but the release gate must keep the complete Codex login flow selected.
      - name: Codex OAuth login persistence
        run: |
          set -o pipefail
          output="$(mktemp)"
          cargo test --locked -p xai-grok-shell --lib \
            auth::openai_codex::tests::full_login_flow_persists_provider_scoped_codex_credential \
            -- --exact --nocapture 2>&1 | tee "$output"
          status="${PIPESTATUS[0]}"
          if [ "$status" -ne 0 ]; then rm -f "$output"; exit "$status"; fi
          if ! grep -qE '^test result: ok\. 1 passed; 0 failed;' "$output"; then
            echo "::error::Codex OAuth login filter did not match exactly one passing test"
            rm -f "$output"; exit 1
          fi
          rm -f "$output"

      # #356: the unit fixtures prove private staging in-process, but only the
      # production binary crosses config loading, auth-provider minting,
      # session publication, and the first authenticated Responses request.
      # Keep this exact ignored test in CI so a broad lib filter cannot report
      # green while never executing the binary-level contract.
      - name: Codex provider prompt publishes one complete session tree
        env:
          GROK_BINARY: ${{ github.workspace }}/target/debug/xai-grok-pager
        run: |
          set -o pipefail
          cargo build --locked -p xai-grok-pager-bin
          output="$(mktemp)"
          cargo test --locked -p xai-grok-shell --test test_auth_provider_e2e \
            codex_provider_prompt_publishes_one_complete_session_tree \
            -- --ignored --nocapture 2>&1 | tee "$output"
          status="${PIPESTATUS[0]}"
          if [ "$status" -ne 0 ]; then rm -f "$output"; exit "$status"; fi
          if grep -qE '^test result: ok\. 0 passed' "$output"; then
            echo "::error::Codex publication E2E filter matched zero tests"
            rm -f "$output"; exit 1
          fi
          rm -f "$output"

      - name: Sampler auth / wire tests
        run: |
          set -o pipefail
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-sampler --test shared_http_wire \
            none_auth_scheme_ -- --nocapture
          run_nonzero -p xai-grok-sampler --lib none_scheme_ -- --nocapture
          run_nonzero -p xai-grok-sampler --lib hostile_injector -- --nocapture
          run_nonzero -p xai-grok-sampler --lib omits_xai_identity -- --nocapture

      - name: Credential relation and telemetry export guards
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-auth --features middleware --lib credential -- --nocapture
          run_nonzero -p xai-grok-telemetry --lib unified_log -- --nocapture
          # #121: for_test is the single place that names SamplingConfig fork
          # fields. Unique substrings — run_nonzero only fails on *zero* matches.
          run_nonzero -p xai-grok-sampling-types --lib \
            sampling_config_for_test_sets_fork_endpoint_trust_to_none -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            sampling_config_for_test_fru_inherits_fork_endpoint_trust_without_naming_it -- --nocapture
          # #245: 400 body preview is secret-free and bounded; unique names.
          run_nonzero -p xai-grok-sampling-types --lib \
            provider_error_body_preview_is_secret_free_and_bounded -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            provider_error_body_preview_is_empty_for_anything_unstructured -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            user_facing_400_surfaces_structured_rejected_field -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            codex_wire_capabilities_omit_reasoning_summary_when_unsupported -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            codex_wire_capabilities_keep_summary_when_catalog_allows_or_is_silent -- --nocapture
          # #259 / #261: catalog compact, truncation, effort, and image-detail
          # fields reach parse + apply (unique names so run_nonzero still binds).
          run_nonzero -p xai-grok-sampling-types --lib \
            codex_wire_capabilities_set_request_truncation_for_token_policy -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            codex_wire_capabilities_read_image_detail_original_fail_closed -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            codex_instructions_receive_catalog_base_instructions -- --nocapture
          # #274: Sol catalog use_responses_lite must reshape the request body.
          run_nonzero -p xai-grok-sampling-types --lib \
            use_responses_lite -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            use_responses_lite -- --nocapture
          # #263: the catalog parser and the single chat-state mutation must
          # both run; xai-chat-state previously had no CI filter (#280).
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_reads_and_validates_truncation_policy -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_reads_auto_compact_token_limit -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_keeps_image_detail_original_and_instructions -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            resolve_auto_compact_token_threshold_uses_catalog_absolute_limit -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            should_auto_compact_honors_catalog_token_limit -- --nocapture
          # #260: a Codex-routed entry whose wire model is not a catalog slug
          # is unready before request I/O; a real slug override stays ready.
          run_nonzero -p xai-grok-shell --lib \
            codex_wire_model_catalog_slug_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_reads_minimal_client_version -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_reads_upgrade_migration -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            tool_result_truncation_policy_catalog_refresh_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            trusted_reminder_suffix_ -- --nocapture
          run_nonzero -p xai-chat-state --lib \
            tool_result_truncation_policy -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            final_relation_observes_hostile_injector_replacement_and_removal -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            post_capture_is_immune_to_resolver_rotation_after_build -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            sampler_request_logs_never_emit_credential_bytes -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            transport_failure_never_emits_query_credential_bytes -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            provider_controlled_stream_error_is_secret_free_for_all_stream_apis -- --nocapture
          # Codex Responses wire invariants are release gates, not incidental
          # coverage. Keep exact filters so removal or renaming fails closed.
          run_nonzero -p xai-grok-sampler --lib \
            codex_transport_normalizes_endpoint_and_isolates_live_headers -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            codex_defaults_enable_parallel_tools_without_overriding_explicit_choice -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            codex_transport_rejects_non_allowlisted_origins_and_query_params -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            codex_mock_request_has_exact_path_and_no_xai_extensions -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            codex_sends_the_prompt_cache_key_the_predicate_promises -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            codex_401_reports_actual_live_request_credential_relation_without_secrets -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            codex_empty_terminal_output_backfills_from_item_done -- --nocapture
          # #304: Codex sometimes omits answer-span annotations. This proves
          # authoritative streamed items survive partial terminal output.
          run_nonzero -p xai-grok-tools --lib \
            codex_web_search_reconciles_partial_terminal_output_for_both_entry_points -- --nocapture
          # #354: web-search must strip xAI identity / internal metadata at
          # third-party origins after every header source has merged.
          run_nonzero -p xai-grok-tools --lib \
            third_party_request_build_strips_merged_identity_headers -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            mixed_case_extra_headers_cannot_bypass_request_build_scrub -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            mixed_case_env_header_names_cannot_bypass_third_party_scrub -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            trusted_xai_request_build_keeps_first_party_headers -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            trusted_proxy_request_build_keeps_first_party_headers -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            third_party_wire_request_has_no_internal_metadata -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            primary_subagent_and_model_switch_request_builds_share_boundary -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            codex_web_search_drops_internal_metadata_from_merged_headers -- --nocapture
          run_nonzero -p xai-grok-mcp --lib \
            callback_errors_never_echo_provider_controlled_credentials -- --nocapture
          # #151: an unlabelled chat-state credential reaching an external origin
          # on `Unknown(CatalogUnavailable)`. Closed in code by #136, and the two
          # tests that pin it matched no filter at all -- so the highest-stakes
          # property in this fork was defended by tests no job ran (#171).
          #
          # Deliberately in this step rather than the auth one above: that step
          # uses bare `cargo test`, where a filter matching zero tests exits 0.
          # For a security property "the filter stopped matching" and "the
          # property still holds" must not look the same, and only `run_nonzero`
          # tells them apart.
          run_nonzero -p xai-grok-shell --lib catalog_unavailable -- --nocapture
          run_nonzero -p xai-grok-shell --lib test_catalog_auth_schemes_and_override -- --nocapture
          # #329: keyless local mock fixtures stay ready; start() stays Bearer.
          run_nonzero -p xai-grok-test-support --lib \
            keyless_local_server_catalog_emits_auth_scheme_none -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            keyless_local_mock_catalog_entry_is_ready_on_loopback -- --nocapture
          # #132: the reader's dup must not be inherited by children. Named
          # explicitly rather than by a `pty_session` prefix: the rest of that
          # module is the hang this change fixes, and this one is the fd leak
          # the fix introduced.
          run_nonzero -p xai-grok-shell --lib \
            dup_fd_is_not_inherited_by_exec_child -- --nocapture
          # #13: env-key normalization. In this step rather than the auth one
          # above because one of these asserts diagnostics carry the normalized
          # *name* and never a value -- and that step uses bare `cargo test`,
          # where a filter matching zero tests exits 0. `env_keys_` selects all
          # seven new tests and their three existing siblings.
          run_nonzero -p xai-grok-shell --lib env_keys_ -- --nocapture
          # #73: strict-storage hardening. One guard proves strict reads stay on
          # the already-open inode, one proves strict writes fail closed on a
          # post-rename owner-only permission assertion failure.
          run_nonzero -p xai-grok-shell --lib \
            strict_read_uses_open_file_handle_when_path_is_swapped_after_open -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            strict_atomic_write_fails_when_post_rename_permission_assertion_fails -- --nocapture
          # #56: trusted project [models]/[model.*] precedence + untrusted
          # hostile-model boundary (no credential/env-key/origin injection).
          run_nonzero -p xai-grok-shell --lib \
            project_model_overlay_precedence_merges_global_repo_and_subdir_sections -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            project_model_overlay_untrusted_repo_cannot_introduce_or_redirect_credentials -- --nocapture
          run_nonzero -p xai-grok-mcp --lib \
            callback_payload_debug_never_emits_code_state_or_issuer -- --nocapture
          run_nonzero -p xai-file-utils --lib \
            final_401_relation_uses_final_wire_credential_and_post_response_snapshot -- --nocapture
          run_nonzero -p xai-file-utils --lib \
            upload_config_debug_redacts_all_secret_carriers -- --nocapture
          run_nonzero -p xai-file-utils --lib \
            git_ancestor_does_not_override_excluded_component -- --nocapture

      - name: Provider docs examples and fixtures
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          # #31: validate executable provider docs. Unique filters so
          # run_nonzero proves each docs guard still runs after refactors.
          run_nonzero -p xai-grok-shell --lib \
            docs_examples_extractor_handles_markers_ignores_unmarked_and_rejects_malformed -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            docs_examples_parse_and_resolve_provider_toml_examples -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            docs_examples_enforce_readiness_and_secret_policy -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            docs_examples_validate_offline_commands_and_links -- --nocapture

      - name: Update channel isolation guards
        # The fork's worst failure mode is a medley binary self-updating into
        # upstream's release channel and replacing itself with the official CLI
        # (issue #71). The release workflow trusts this job, and it only checks
        # that the distribution marker is present in the shipped binary — these
        # are what prove the marker actually stops an update.
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-update --lib dist_channel -- --nocapture
          run_nonzero -p xai-grok-update --lib self_update -- --nocapture
          run_nonzero -p xai-grok-update --test test_dist_channel_gate -- --nocapture
          # Same marker, one layer out: the release workflow greps
          # `version --json` for `distChannel` to prove MEDLEY_CHANNEL reached
          # the build, so a renamed or dropped key fails a release rather than
          # a test. `channel`, `upstreamBase`, and `buildTarget` have no guard
          # at all outside this filter (issue #29).
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            version_json_payload_ -- --nocapture

      - name: Leader lock identity (Linux-only /proc probe)
        # The leader auto-kill path proves "is this OUR process" by checking
        # that the PID holds an open descriptor on the lock file (issue #158).
        # The safety half — never evict a process we cannot prove is ours — is
        # a pure unit test that runs everywhere. The POSITIVE half needs
        # /proc/<pid>/fd, so it is #[cfg(target_os = "linux")] and cannot run
        # on the macOS machines this project is developed on. Without this
        # step it would run nowhere at all, and "eviction can now fire" would
        # ship with zero executions of the test proving it can.
        #
        # run_nonzero fails on a zero-match filter, so this also catches the
        # test later being renamed, moved, or cfg'd out — the exact way it
        # silently stopped being covered before.
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib holder_has_lock_file_open -- --nocapture

      - name: Codex Ultra client and wire contracts
        # #357: Ultra is a client orchestration tier that must lower to the
        # provider-supported `max` wire value. Keep each boundary named in full
        # so an unrelated Codex or Responses test cannot make this lane green.
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-sampler --lib \
            codex_ultra_preparation_preserves_max_wire_effort_and_injects_marker_once -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            codex_non_ultra_preparation_does_not_inject_proactive_marker -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            generic_responses_never_receive_codex_ultra_proactive_marker -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            test_typed_response_max_reasoning_effort_stamped_on_assistant_as_max -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            test_response_wrapper_keeps_client_ultra_separate_from_typed_max -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_preserves_mixed_ultra_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_preserves_ultra_only_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            subagent_reasoning_override_preserves_ultra_when_child_menu_offers_it -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            pager_model_state_exposes_ultra_in_the_menu -- --nocapture
          # #360: `/new` must use the same implicit built-in effort menu as
          # model switching when support is explicit but the catalog list is empty.
          run_nonzero -p xai-grok-shell --lib \
            prepared_new_session_plan_preserves_selected_effort_with_implicit_menu -- --nocapture

      - name: Restricted subagent capability boundaries
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-tools --lib \
            restricted_capability_matrix_finalizes_and_dispatches_with_one_policy -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            readonly_gateway_target_rejects_unclassified_and_external_mutation -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            issue39_deploy_app_requires_all_capability_mode -- --nocapture
          # #181: a route that declares its own Authorization must keep it when
          # an ambient bearer becomes available mid-session. Named in full --
          # the property is about the *transition*, and a prefix that also
          # matched the steady-state cases would go green on those alone.
          run_nonzero -p xai-grok-tools --lib \
            first_party_declared_authorization_header_survives_mid_session_login -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            tool_result_truncation_policy_runner_records_trusted_suffix_provenance -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            structured_completion_reminders_carry_every_producer_id -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            tool_result_truncation_policy_proxy_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            restricted_gateway_snapshot_exposes_only_trusted_permitted_tools_per_session -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            restricted_children_reject_agent_owned_mcp_servers_before_session_spawn -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_cleanup_rejected_spawn_worktree_removes_only_fresh_worktrees -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_harness_rejection_cleans_fresh_worktree_before_handoff -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_pre_handoff_guard_cleans_worktree_on_panic -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue14_spawn_refresh_replaces_stale_parent_capabilities -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue14_spawn_refresh_parent_actor_unavailable_is_actionable_error -- --nocapture
          run_nonzero -p xai-chat-state --lib \
            prepared_model_state_never_tears_routing_from_credentials -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            keeps_endpoint_and_bound_secret_when_name_collides_with_an_external_model -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            attach_restore_blocks_exact_key_reuse_and_unique_route_ambiguity -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            empty_catalog_keeps_persisted_identity_pending_instead_of_rejecting_load -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            prompt_recovery_rejects_a_remapped_model_with_an_incompatible_harness -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            identity_backed_prompt_recovery_requires_persisted_harness_evidence -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            cold_spawn_reconciled_route_rejects_incompatible_harness -- --nocapture
          # #300: an inherited Codex child resumes the already-admitted tuple;
          # durable legacy lineage is reconciled before migration, route/harness
          # drift still fails closed, and fresh overrides still preflight.
          run_nonzero -p xai-grok-subagent-resolution --lib \
            resume::tests -- --nocapture
          run_nonzero -p xai-grok-subagent-resolution --lib \
            native_route:: -- --nocapture
          run_nonzero -p xai-grok-agent --lib \
            from_json_rejects_empty_models_entry -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            format_agent_detail -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            resume_model_lineage_tests -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            durable_legacy_resume_ -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            completed_resume_source_carries_committed_model_lineage -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            pre_promotion_failure_does_not_mask_durable_resume_fallback -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_verbatim_fork_tools_intersect_session_admission -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_rebuild_respects_capability_mode_ceiling -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            explicit_gateway_cache_invalidation_waits_for_all_session_admissions -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_gateway_invalidation_includes_registered_child_sessions -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue39_aborted_explicit_gateway_refresh_recovers_waiters -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            gateway_refresh_fence_prevents_stale_snapshot_reconciliation -- --nocapture
          run_nonzero -p xai-grok-shell-session-support --lib \
            explicit_gateway_refresh_fence_clears_only_on_fresh_commit -- --nocapture
          run_nonzero -p xai-grok-shell-session-support --lib \
            legacy_config_invalidation_preserves_gateway_catalog -- --nocapture
          run_nonzero -p xai-grok-shell-session-support --lib \
            issue39_abort_gateway_tool_fetch_recovers_waiters -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            subagent_trusted_tool_capabilities_render_in_human_and_json_views -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            resolve_session_toolset_mcp_kind_none_dropped_under_readonly -- --nocapture
          # #354: workspace proxy-header injection is first-party only.
          run_nonzero -p xai-grok-workspace --lib \
            build_proxy_headers_ -- --nocapture
          run_nonzero -p xai-computer-hub-sdk --lib \
            unavailable_handlers_are_absent_from_publish_and_dispatch -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            dispatch_configure_mcp -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            repo_configs_present_detects_grok_config_models -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            repo_configs_present_false_for_empty_models_tables -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            qualified_handler_fails_closed_when_session_generation_expires -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            unrestricted_generation_guard_rejects_reused_session_id -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            drop_cleanup_clears_old_generation_resources_written_in_flight -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            local_session_reservation_ -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            session::anchored -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            session::fresh_publication -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            session::publication_parent -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            session::id_lock -- --nocapture
          run_nonzero -p xai-circuit-breaker --lib \
            edge_client_retries_transient_edge_codes_but_not_origin_tls -- --nocapture
          run_nonzero -p xai-sqlite-journal --lib \
            apply_with_retry -- --nocapture
          run_nonzero -p xai-tty-utils --lib \
            git_command_locking -- --nocapture
          run_nonzero -p xai-tool-types --lib \
            background_spawn_notice_keeps_poll_hint_and_open_parent_work -- --nocapture
          run_nonzero -p xai-tool-types --lib \
            continue_parent_work_detects_prior_exec_and_skips_review_only -- --nocapture
          run_nonzero -p xai-computer-hub-sdk --lib \
            connection:: -- --nocapture
          run_nonzero -p xai-computer-hub-sdk --lib \
            server:: -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            permission::auto_mode:: -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            permission::manager:: -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            permission::prompter:: -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            permission::types:: -- --nocapture
          # Upstream moved these tests into an external `restore_fetch_tests.rs`
          # via `#[path = "restore_fetch_tests.rs"] mod tests;` — the *file* was
          # renamed, the module path was not, and a filter naming the file
          # matched nothing. `run_nonzero` is what noticed.
          #
          # It stops at `restore_fetch::` rather than `restore_fetch::tests::`
          # so that both readers agree. Cargo selects the same tests either way,
          # but the #171 guard approximates a module path by turning `::` into
          # `/` and looking for it in the file path, which cannot see through
          # `#[path]`: it would look for `restore_fetch/tests` in a file called
          # `restore_fetch_tests.rs` and report every test here as running
          # nowhere. `restore_fetch::` is a substring of both.
          run_nonzero -p xai-grok-workspace --lib \
            restore_fetch:: -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            session::git:: -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            workspace_ops:: -- --nocapture
          # Session publication is intentionally side-effect free until the
          # final gate opens. Keep every deferred subsystem on the hot-path
          # lane so a renamed filter cannot silently drop this contract.
          run_nonzero -p xai-grok-memory --lib \
            deferred_watcher_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            deferred_relay_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            dropping_pending_relay_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            notification_bridge_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            acknowledged_removal_queued_before_abort_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            published_write_post_commit_sync_failure_preserves_visible_directory -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            published_copy_post_commit_sync_failure_preserves_visible_target -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            aborted_fresh_publication_drops_queued_mutations_and_sync_side_effects -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            remote_pull_publication_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            fetched_session_rejects_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            extensions::session_state:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            symlinked_cwd_parent_without_touching_outside_tree -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            stale_public_marker_is_preserved_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            no_replace_ -- --nocapture
          run_nonzero -p xai-hunk-tracker --lib \
            gated_spawn_cancelled_before_readiness_ -- --nocapture
          # `xai-fast-worktree` has no default features and its `db` module is
          # gated on `metadata`, so without this the filter compiles a crate
          # that contains none of the tests it names.
          run_nonzero -p xai-fast-worktree --features metadata --lib \
            db::tests:: -- --nocapture
          run_nonzero -p xai-file-utils --lib \
            storage_client:: -- --nocapture
          run_nonzero -p xai-grok-shell-base --lib \
            ensure_owner_only_directory_ -- --nocapture
          run_nonzero -p xai-grok-shell-base --lib \
            anchored_directory:: -- --nocapture
          run_nonzero -p xai-grok-auth --lib \
            bearer_fragment:: -- --nocapture
          run_nonzero -p xai-grok-auth --lib \
            internal_metadata:: -- --nocapture
          run_nonzero -p xai-grok-markdown --lib \
            render:: -- --nocapture
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            cache_ -- --nocapture
          run_nonzero -p xai-grok-pager-render --lib \
            terminal::tmux:: -- --nocapture
          run_nonzero -p xai-grok-pager-render --lib \
            theme:: -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            retry:: -- --nocapture
          # `--test <target>` is already the restriction. The positional that
          # used to follow named the *target* again, and an integration test's
          # target name is not a module inside it -- these seven live at the
          # binary's root, so `cf_edge_error_message::` selected none of them.
          run_nonzero -p xai-grok-sampler --test cf_edge_error_message -- --nocapture
          run_nonzero -p xai-grok-sampling-types --lib \
            error:: -- --nocapture
          run_nonzero -p xai-grok-sandbox --lib \
            deny::glob:: -- --nocapture
          run_nonzero -p xai-grok-shared --lib \
            ui_config:: -- --nocapture
          run_nonzero -p xai-grok-telemetry --lib \
            events:: -- --nocapture
          run_nonzero -p xai-grok-telemetry --lib \
            external:: -- --nocapture
          run_nonzero -p xai-grok-telemetry --lib \
            startup:: -- --nocapture
          run_nonzero -p xai-grok-test-support --lib \
            resources:: -- --nocapture
          # #296: loopback PTY model fixtures must explicitly opt out of auth;
          # otherwise credential-origin hardening correctly makes them unready.
          run_nonzero -p xai-grok-test-support --lib \
            mock_model_emits_explicit_auth_scheme -- --nocapture
          run_nonzero -p xai-grok-pager-pty-harness --lib \
            replacement_models_keep_keyless_loopback_default -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            unresolved_empty_current_model_uses_live_fail_closed_sampling_config -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            implementations:: -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            registry:: -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            types:: -- --nocapture
          run_nonzero -p xai-grok-tools --lib \
            util:: -- --nocapture
          run_nonzero -p xai-grok-workspace-types --lib \
            rpc::repos:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::activity:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::app:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::auth_method:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::chat_modes:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::config:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::config_model_override_parse:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::docs_examples_tests:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::ext_parsers:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::feedback_client:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::folder_trust:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::handlers:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::model_providers:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::models:: -- --nocapture
          # EnvGuard tests in this module are #[serial], but sibling tests still
          # mutate GROK_HOME / XAI_API_KEY in parallel and steal their isolation.
          run_nonzero -p xai-grok-shell --lib \
            agent::mvp_agent:: -- --nocapture --test-threads=1
          # #354: identity headers are not injected for third-party origins.
          run_nonzero -p xai-grok-shell --lib \
            inject_proxy_headers_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::otel_gate:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::proxy:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::relay:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::restore_code:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::roster:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::server:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::session_config:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::session_registry_client:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::storage_client_tests:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::subagent:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::subscription_check:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            agent::update_chunk_merge:: -- --nocapture
          # Auth-provider tests spawn credential-helper subprocesses. Keep them
          # parallel, but bound libtest fan-out so host pressure cannot turn an
          # unrelated helper's first mint into a resource-exhaustion flake.
          run_nonzero -p xai-grok-shell --lib \
            auth:: -- --nocapture --test-threads=4
          # Codex fixtures previously changed GROK_AUTH_PATH process-wide.
          # Keep this broad filter on the default parallel harness: serial-only
          # execution masks cross-module auth-store contamination (#343).
          run_nonzero -p xai-grok-shell --lib \
            codex_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            config:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            inspect:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            leader:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            managed_config:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            sampling:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            session:: -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            util:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            app:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            disk_usage_cmd:: -- --nocapture
          # `xai-grok-config` had no filter at all, so every test in it ran
          # nowhere. `display::` owns the state-directory label rules, which
          # is exactly the fork behaviour a sync is most likely to break --
          # `disk_usage_cmd::` above is one of its callers, and it caught a
          # regression here that this crate's own tests could not report.
          run_nonzero -p xai-grok-config --lib \
            display:: -- --nocapture
          run_nonzero -p xai-grok-config --lib \
            validation:: -- --nocapture
          # Same `#[path]` move as `restore_fetch::` above, and stopped one
          # segment short for the same reason: the file is `fs_size_tests.rs`,
          # the module is `fs_size::tests`, and only `fs_size::` is a substring
          # of both. This filter named the file and matched zero tests — which
          # `run_nonzero` catches, while the #171 guard called the same line
          # covered. Wrong in both directions at once.
          run_nonzero -p xai-grok-pager --lib \
            fs_size:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            headless:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            input:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            notifications:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            pty_wrap:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            scrollback:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            settings:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            slash:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            views:: -- --nocapture
          # Same shape as `cf_edge_error_message` above: the positional repeated
          # the target name, which is not a module path inside the binary.
          run_nonzero -p xai-grok-pager --test grok_home_paths -- --nocapture
          run_nonzero -p xai-grok-pager --test signal_errno_preservation -- --nocapture
          run_nonzero -p xai-grok-pager --test settings_e2e -- --nocapture
          run_nonzero -p xai-grok-shell --test acp_session_setup_wire \
            acp_session_setup_conformance -- --nocapture
          run_nonzero -p xai-grok-sandbox --lib \
            resolve_failure_refuses_for_deny_carrying_devbox_extends -- --nocapture
          run_nonzero -p xai-grok-sandbox --lib \
            bwrap_reexec_for_profile_skips_plan_inside_bwrap -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            terminal::local_terminal:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            acp:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            actions:: -- --nocapture
          # This one was wrong twice. `--test pty_e2e` names a target that does
          # not exist -- upstream splits the PTY suites into `pty_e2e_smoke`,
          # `_queue`, `_minimal`, `_config_ui` and so on, each an explicit
          # `[[test]]` entry, and `tests/pty_e2e/` has no `main.rs` to make the
          # directory one. `cargo test` errors out rather than reporting a
          # count, so this never even reached the zero-match check. And the
          # positional selected nothing in the target that *does* exist: those
          # cases sit under `minimal::`, not `pty_e2e::`.
          #
          # 30 of the 31 are `#[ignore]`d without `PAGER_BINARY`, so what this
          # buys is the target compiling plus its one runnable case -- which is
          # the whole value, since a broken PTY harness fails to build.
          run_nonzero -p xai-grok-pager --test pty_e2e_minimal -- --nocapture
          # `pty_e2e_queue` was named by nothing. It only became visible once
          # the #171 guard stopped agreeing with `--test pty_e2e` -- the two
          # readers were wrong together, so neither reported it.
          run_nonzero -p xai-grok-pager --test pty_e2e_queue -- --nocapture

      - name: Agent serve exposure and authentication
        run: |
          set -o pipefail
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            issue8_agent_serve_security_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_gateway_queue_rejects_when_full -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_oversized_trailing_ping_closes_with_1009 -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_oversized_empty_after_trim_closes_with_1009 -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue41_bound_ws_queues_oversized_non_utf8_binary_closes_with_1009 -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue8_agent_serve_security_ -- --nocapture
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            issue8_agent_serve_security_ -- --nocapture

      - name: "Leader kill safety (issue #167)"
        # `leaders kill` used to ask "is this process named grok?", which is
        # false for the shipped `medley` binary — so it deleted a LIVE leader's
        # lock and socket without signalling it, and the next client acquired
        # the flock on a fresh inode: two live leaders from a command whose
        # purpose is to leave none.
        #
        # These pin the two invariants that replaced it: a socket-verified live
        # leader is signalled and its files are never touched, and a lock file
        # is reclaimed only when no live process holds its flock. Neither
        # consults a process name, so neither can rot on the next rename.
        #
        # Needed because ci.yml has no unfiltered job (#171): the only other
        # xai-grok-pager-bin invocation filters on `issue8_agent_serve_security_`,
        # so without this step these tests would run nowhere. `run_nonzero`
        # fails on a zero-match filter, so renaming them breaks CI loudly
        # instead of silently dropping the coverage.
        run: |
          set -o pipefail
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            leaders_kill -- --nocapture
          # Unique selector for #183: proves the race-repro test itself runs
          # (shared `leaders_kill` prefixes would stay green if it were deleted).
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            leaders_kill_issue183_interleaving_preserves_new_leader_socket -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            leader::lock::tests::reclaim -- --nocapture

      - name: Shell auth + readiness meta
        run: |
          set -o pipefail
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib none_auth_scheme -- --nocapture
          run_nonzero -p xai-grok-shell --lib acp_model_meta_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib agent::auth_method -- --nocapture
          run_nonzero -p xai-grok-shell --lib config_model_override_parse -- --nocapture
          run_nonzero -p xai-grok-shell --lib model_overrides_ -- --nocapture
          # #13: env_key trim/validate/dedupe + path-specific invalid-name warnings.
          # Unique prefixes so run_nonzero proves the new tests are selected.
          run_nonzero -p xai-grok-shell --lib env_keys_normalize_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib env_keys_diagnostics_show_normalized_names_only -- --nocapture
          run_nonzero -p xai-grok-shell --lib env_keys_invalid_entry_emits_path_specific_config_warning -- --nocapture
          run_nonzero -p xai-grok-shell --lib read_parent_sampling_config_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib handle_set_session_model_clears_credentials_for_none -- --nocapture
          run_nonzero -p xai-grok-shell --lib catalog_key -- --nocapture
          run_nonzero -p xai-grok-shell --lib auth_scheme -- --nocapture
          run_nonzero -p xai-grok-shell --lib sampling_config_for_unready -- --nocapture
          run_nonzero -p xai-grok-shell --lib credential_header -- --nocapture
          run_nonzero -p xai-grok-shell --lib invalid_auth_scheme -- --nocapture
          run_nonzero -p xai-grok-shell --lib relation_matrix_is_secret_free -- --nocapture
          run_nonzero -p xai-grok-shell --lib callback_errors_never_echo_provider_controlled_values -- --nocapture
          run_nonzero -p xai-grok-shell --lib snapshot_never_derives_stable_ids_from_credentials -- --nocapture
          # #132: CLOEXEC regression guard; full test path keeps the filter
          # unique so #171 can prove this exact test is selected.
          run_nonzero -p xai-grok-shell --lib \
            terminal::pty_session::tests::dup_fd_is_not_inherited_by_exec_child -- --nocapture

      - name: "Substituted default model meta (#131)"
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            absent_configured_default_is_substituted_and_reported -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            present_but_not_selectable_configured_default_is_substituted_and_reported -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            honoured_configured_default_reports_no_substitution -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            campaign_driven_default_is_not_reported_as_the_users_choice -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            configured_unready_default_already_publishes_its_reason_per_model -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            initialize_publishes_substituted_default_model_meta -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            initialize_omits_substituted_default_model_meta_when_honoured -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            reselect_if_missing_clears_stale_substitution_on_early_return -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            clear_wipes_substituted_preference -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            models_update_meta_clears_substitution_after_catalog_lands -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            warm_cache_refresh_does_not_reseat_over_user_model_pick -- --nocapture
          # #358: 400 detail extraction must tolerate an optional reason phrase.
          run_nonzero -p xai-grok-shell --lib \
            remainder_after_http_400_prefix -- --nocapture

      - name: Pager model picker / readiness
        run: |
          set -o pipefail
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-pager --lib slash::commands::model:: -- --nocapture
          run_nonzero -p xai-grok-pager --lib hard_blocks_ -- --nocapture
          run_nonzero -p xai-grok-pager --lib catalog_miss -- --nocapture
          run_nonzero -p xai-grok-pager --lib toctou -- --nocapture
          # The three tests this change fixes matched none of the filters
          # above, so registering their models bought nothing: they ran in no
          # job at all (#171). This selects them and the six sibling
          # deferred-switch tests, which belong to this step by subject and
          # were equally unrun.
          run_nonzero -p xai-grok-pager --lib \
            deferred_switch_ -- --nocapture
          # #11: the two persist-failure mirrors. These live in the pager's
          # dispatch, not the shell -- the issue is about model persistence and
          # the tests are about what the pager mirrors after a failed write.
          run_nonzero -p xai-grok-pager --lib \
            preferred_model_persist_failure -- --nocapture
          # #335 / #346 / #358 / #359: live catalog coherence, settings
          # disablement, 4xx retry banners, and /model initial highlight.
          # `run_nonzero`, not a bare `cargo test`: this step no longer has a
          # single unguarded invocation, so a filter that stops matching fails
          # the job instead of reporting a green `0 passed`.
          run_nonzero -p xai-grok-pager --lib \
            unavailable_resident -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            models_update_refreshes_open_settings -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            model_auto_switched_reconciles -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            settings_model_picker_enter_on_disabled -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            settings_model_picker_mouse_on_disabled -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            dynamic_enum_choices_marks_unavailable -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            provider_request_failed_http -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            provider_http_400 -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            fresh_model_suggestions_highlight -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            model_suggestion_refresh_preserves -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            nonempty_fuzzy_model_query -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            preferred_model_row_falls_back -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            refresh_open_settings_modals_exits_dynamic_picker_when_highlighted_model_disappears -- --nocapture

      - name: 'Orphaned switch transaction via synthesized switch (issue #239)'
        run: |
          # The stricter of the two `run_nonzero` shapes in this file: it reads
          # cargo's own status out of PIPESTATUS before looking at the output.
          # The grep-only shape cannot tell a failing run from a passing one
          # when an invocation names more than one target -- one binary's
          # `ok. N passed` line satisfies it while another binary failed.
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero --manifest-path crates/codegen/xai-grok-pager/Cargo.toml --lib \
            synthesized_switch_does_not_clear_transaction_with_live_request_id -- --nocapture

      # #170: the dashboard's mirror obligations. A refused pre-session pick
      # completes with a toast on the new agent; whenever the view stays on
      # the dashboard the caller must mirror it onto the dashboard's error
      # slot. The two mirror guards landed with this subsystem's fix but
      # matched no filter, so the obligation's tests ran nowhere (#171); the
      # new button-path test pins why THAT path may discard the refusal
      # signal. Full unique names so run_nonzero proves each one still runs.
      - name: 'Dashboard refused-pick mirror obligations (issue #170)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-pager --lib \
            dashboard_dispatch_refused_pick_mirrors_error_and_drops_cli_stash -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            dashboard_confirm_worktree_refused_pick_mirrors_error_toast -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            dashboard_new_agent_button_refused_pick_toast_follows_the_view -- --nocapture
          # `xai-fsnotify` had no filter at all, so nothing in it ran in CI --
          # including the checkout classifier that decides where the file
          # watcher stops. That is why #278 shipped: the test which catches it
          # was never executed anywhere except a developer's macOS machine.
          run_nonzero -p xai-fsnotify --lib \
            checkout:: -- --nocapture

      # #159: a runtime-only model was judged absent by a config-only lookup,
      # and the turn stripped its credentials. Filters written out rather than
      # looped, so the #171 check can see them. Quoted name: an unquoted `#159`
      # truncates at the comment marker.
      - name: 'Runtime-catalog lookup and memo generation (issue #159)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib runtime_catalog -- --nocapture
          run_nonzero -p xai-grok-shell --lib runtime_only_catalog -- --nocapture
          run_nonzero -p xai-grok-shell --lib config_only_miss -- --nocapture
          run_nonzero -p xai-grok-shell --lib memoized_as_definite -- --nocapture
          run_nonzero -p xai-grok-shell --lib catalog_refresh_that_drops -- --nocapture
          run_nonzero -p xai-grok-shell --lib bundled_fallback_rebuild -- --nocapture
          # `agent::models` as a module filter: the generation asserts added to
          # the pre-existing `rebuild_updates_models_and_available` live there
          # and matched no name-based filter, so that guard ran nowhere. A new
          # assertion in an old unfiltered test is invisible to the #171 check,
          # which looks for added `#[test]` attributes.
          run_nonzero -p xai-grok-shell --lib agent::models -- --nocapture

      # #50 and #54 are the same subsystem from two directions -- crash
      # consistency across the intent write/rename boundary, and the narrow
      # windows around owner-bound load markers and dropped acks. They added
      # separate steps with an overlapping filter, so they are one step here
      # and `dropped_model_switch_ack_...` appears once rather than twice.
      # Filters are unique names, so run_nonzero proves each is selected.
      - name: 'Model-switch persistence boundaries and narrow windows (issues #50, #54)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            dropping_newer_duplicate_load_keeps_older_wait_marker_alive -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            dropped_model_switch_ack_preserves_live_candidate_generation -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            model_switch_recovers_new_generation_after_summary_materialization -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            restart_preserves_unique_route_identity_against_removed_key_reuse -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            push_pull_resume_preserves_unique_route_across_key_reuse -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            model_metadata_update_keeps_id_and_identity_atomic -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            fork_registration_remote_pull_preserves_inherited_model_identity_and_harness -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            copy_session_data_with_model_override -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue345_fork_rewrites_parent_compaction_transcript_hint -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue345_checkpoint_rebind_runs_on_deserialized_items -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue423_fork_preserves_authored_summary_text_verbatim -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            fork_rebinds_checkpoint_hint_without_touching_quoted_transcript_paths -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            fork_and_import_reject_symlinked_encoded_cwd_parent -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            init_session_rejects_symlinked_encoded_cwd_occupant_without_summary -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            model_switch_interruption_before_intent_rename_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            model_switch_interruption_after_intent_rename_ -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            list_sessions_recovers_pending_model_switch_intent_before_loading_summary -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            list_sessions_recent_recovers_pending_model_switch_intent_before_loading_summary -- --nocapture

      # #166: these assert on colour, and used to inherit the ambient colour
      # level. Run them under both settings of NO_COLOR -- one pass alone would
      # not have caught the original bug, because it only showed up on the axis
      # CI never varied. Quoted name: an unquoted `#166` truncates at the
      # comment marker.
      - name: 'Colour-asserting pager tests, both NO_COLOR settings (issue #166)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          # Filters written out rather than looped over a shell variable, so
          # they stay greppable in this file -- the #171 check parses these
          # lines, and a filter built at runtime is invisible to it.
          #
          # Do not use bare `teal`: Cargo substring-matches, and `steal` /
          # `steals` / `resteal` contain `teal`, as does
          # `teal_highlighting_on_second_line` (which skips its colour assert
          # under NO_COLOR). `keeps_teal` + `each_teal` select only the four
          # fixed skill-accent tests that filter on accent_skill spans.
          run_nonzero -p xai-grok-pager --lib keeps_teal -- --nocapture
          run_nonzero -p xai-grok-pager --lib each_teal -- --nocapture
          run_nonzero -p xai-grok-pager --lib invalid_token_ranges_are_dropped -- --nocapture
          run_nonzero -p xai-grok-pager --lib token_on_second_logical_line -- --nocapture
          run_nonzero -p xai-grok-pager --lib search_bar_cursor -- --nocapture
          # Made hermetic by this change like the others, but pre-existing, so
          # the #171 check cannot see it: that check reads the diff for a new
          # `#[test]`, and this test is not new -- only its contract is.
          run_nonzero -p xai-grok-pager --lib \
            user_prompt_band_is_semantic -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            colour_asserting_tests_do_not_use_theme_current -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib keeps_teal -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib each_teal -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib invalid_token_ranges_are_dropped -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib token_on_second_logical_line -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib search_bar_cursor -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib \
            user_prompt_band_is_semantic -- --nocapture
          NO_COLOR=1 run_nonzero -p xai-grok-pager --lib \
            colour_asserting_tests_do_not_use_theme_current -- --nocapture

      # #162: the restore-direction and the tombstone-wall cases. The
      # pre-existing `workflow_restore_rejects_symlinks_and_caps_run_count`
      # stays green under both defects, so `workflow_restore` as one filter
      # covers all three and the two new ones carry the protection. Quoted
      # name: an unquoted `#162` truncates at the comment marker.
      - name: 'Workflow restore ordering and scan budget (issue #162)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib workflow_restore -- --nocapture

      # #10: catalog IDs and wire slugs are distinct identities. These filters
      # must be unique so run_nonzero proves each guard test still executes.
      # Quoted name: an unquoted `#10` truncates at the comment marker.
      - name: 'Catalog ID vs wire slug restore/switch guards (issue #10)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            auto_compact_threshold_resolves_by_catalog_id_not_wire_slug -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            resolve_catalog_key_none_when_slug_is_ambiguous -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            selectable_catalog_resolution_reports_ambiguous_slug -- --nocapture

      # #165: abandoned sampling attempts must not enter the FTS index.
      # Unique filters so run_nonzero proves each new test is selected.
      # Quoted name: an unquoted `#165` truncates at the comment marker.
      - name: 'Abandoned attempt text omitted from search index (issue #165)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_discards_abandoned_retry_attempt_phrase -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_discards_attempt_discarded_retraction_phrase -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_still_flushes_on_rewind_marker_boundary -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_still_flushes_on_compaction_checkpoint_boundary -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            collect_assistant_text_completed_turn_still_indexes_across_hook_annotation -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            single_pass_omits_abandoned_retry_attempt_from_fts_content -- --nocapture

      # #180: an ambient xAI session JWT under a post-strip `ExplicitHeader`
      # label reaching a third-party origin. Both halves of the fix are
      # independently mutation-proven, so both filters are needed -- reverting
      # either one reddens only its own test. Quoted name: an unquoted `#180`
      # truncates at the comment marker.
      - name: 'Ambient credential label coupling (issue #180)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-sampler --lib \
            ambient_token_labelled_explicit_header -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            dual_auth_gateway_model_key_plus_declared_header -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            seeded_ambient_key_must_not_keep_a_post_strip -- --nocapture
          # Seam half of #180: reconstruct / parent-inherit must not invent
          # ExplicitHeader from the maps while keeping api_key. The sampler
          # dual_auth_gateway_… test never reaches these functions.
          run_nonzero -p xai-grok-shell --lib \
            reconstruct_full_config_ready_dual_auth_keeps_model_api_key -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            read_parent_sampling_config_dual_auth_keeps_model_api_key -- --nocapture

      # #136 step 4: L3 refuses unlabelled / inconsistent bound auth from the
      # material itself, not from a self-reported ambient label alone. Unique
      # filters so run_nonzero proves each new test is selected (#171).
      - name: 'L3 provenance-required and inconsistency (issue #136 step 4)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-sampler --lib \
            unlabelled_bound_auth_on_external_origin_requires_provenance -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            unlabelled_bound_auth_on_https_first_party_xai_still_constructs -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            missing_or_none_label_with_bound_material_is_inconsistent -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            explicit_header_label_with_separate_api_key_is_inconsistent -- --nocapture

      # #123: user-declared trusted origin for ambient xAI credentials
      # (self-hosted gateways). Unique filters so run_nonzero proves each new
      # test is selected (#171). Written out rather than looped so the #171
      # check can see them. Quoted name: an unquoted `#123` truncates at the
      # comment marker.
      - name: 'User-declared trusted xAI origins (issue #123)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-sampler --lib \
            user_declared_trust_allows_labelled_ambient_only_over_https -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            user_declared_trust_does_not_smuggle_unlabelled_material -- --nocapture
          run_nonzero -p xai-grok-sampler --lib \
            user_declared_trust_keeps_external_metadata_boundary -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            trusted_xai_origins_parse_normalizes_origin -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            trusted_xai_origins_parse_rejects_non_https_and_credential_shaped_entries -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            trusted_xai_origins_rejected_rendering_strips_credential_hiding_places -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            trusted_xai_origins_ignored_in_untrusted_layers -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            trusted_xai_origins_matches_exact_origin_only -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            trusted_xai_origins_layers_union_and_dedupe -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            choke_point_declared_trusted_origin_keeps_ambient_credential -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            choke_point_declaration_matches_exact_origin_only -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            readiness_honours_declared_origin_only_where_declared -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            project_config_trusted_origins_declaration_is_reported_inert -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            a_trusted_repo_still_cannot_declare_trusted_xai_origins -- --nocapture

      # #291: the first Codex catalog load can happen after the pager enters
      # Tokio. The parent test launches a fresh copy of the test binary so both
      # the blocking client and its first authenticated request run cold.
      - name: 'Codex catalog first use under Tokio (issue #291)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_runtime_first_fetch_ -- --nocapture

      # #160: six of the seven tests this branch originally added matched no
      # filter. The seventh, `web_search_resolves_an_env_backed_credential_header`,
      # is already selected by the existing `credential_header` filter. Written
      # out rather than looped so the #171 check can see them. Quoted name: an
      # unquoted `#160` truncates at the comment marker.
      - name: 'web_search env header forwarding (issue #160)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib spawn_gates_web_search -- --nocapture
          run_nonzero -p xai-grok-shell --lib spawn_forwards_env_http_headers -- --nocapture
          run_nonzero -p xai-grok-shell --lib web_search_env_header_overrides -- --nocapture
          run_nonzero -p xai-grok-shell --lib env_backed_credential_header -- --nocapture
          # Also this PR's earlier commit, whose three tests matched nothing either.
          run_nonzero -p xai-grok-shell --lib keeps_web_search_and_is_told_nothing -- --nocapture
          run_nonzero -p xai-grok-shell --lib credential_less_route -- --nocapture
          run_nonzero -p xai-grok-shell --lib invents_no_bearer -- --nocapture
          # Review follow-up: resolve→SamplerConfig map copy, and the
          # bearer_resolver arm of has_usable_credential.
          run_nonzero -p xai-grok-shell --lib survives_into_sampler_config -- --nocapture
          run_nonzero -p xai-grok-shell --lib accepts_a_bearer_resolver_alone -- --nocapture
          # #57 follow-up: web_search must fall back to [models].default and keep
          # model-scoped credential provenance when xAI ambient auth is absent.
          run_nonzero -p xai-grok-shell --lib falls_back_to_models_default_and_keeps_model_scoped_credential -- --nocapture

      # #161: the web_search disable notice now rides the session response
      # `_meta` instead of a notification. Two filters because the tests split
      # Exclusive substrings for the #161 tests only. A shared prefix like
      # `web_search_disable` also matches preexisting #57 config/renderer tests,
      # so `run_nonzero` would stay green if the new tests were deleted (#171).
      - name: 'web_search disable notice (issue #161)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib session_meta_publishes_the_web_search_disable -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            session_load_recompute_updates_web_search_notice_for_ready_unusable_and_unknown -- --nocapture
          run_nonzero -p xai-grok-shell --lib session_meta_keeps_web_search_notices_session_scoped -- --nocapture
          run_nonzero -p xai-grok-shell --lib take_session_drops_the_web_search_notice -- --nocapture
          run_nonzero -p xai-grok-pager --lib web_search_disabled_meta -- --nocapture
          run_nonzero -p xai-grok-pager --lib no_web_search_notice -- --nocapture
          run_nonzero -p xai-grok-pager --lib parse_session_web_search_disabled -- --nocapture
          run_nonzero -p xai-grok-pager --lib headless_web_search_notice -- --nocapture

      # #167: pure seams split out of `kill_leaders` so summary/exit and PID
      # guards are unit-testable. The earlier "Leader kill safety" step already
      # selects `leaders_kill` + `reclaim_*` invariants; this step covers the
      # three helpers plus the failed-signal counter contract. Quoted name: an
      # unquoted `#167` truncates the YAML value at the comment marker.
      - name: 'leaders kill summary and PID guard (issue #167)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            format_kill_leaders_summary -- --nocapture
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            leader_kill_action -- --nocapture
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            format_leader_held_guidance -- --nocapture
          # Unique filter: proves the failed-kill → left_alone contract runs
          # (substring shared with `leaders_kill` alone would not).
          run_nonzero -p xai-grok-pager-bin --bin xai-grok-pager \
            leaders_kill_failed_kill_counts -- --nocapture
          # Unique filter: proves startup kill PID plausibility bounds run.
          run_nonzero -p xai-grok-shell --lib \
            startup_kill_socket_pid_plausibility_bounds -- --nocapture
          # `reclaim_` rather than the NotFound test alone: this branch's two
          # mutation-proven invariant tests (`reclaim_removes_a_lock_no_process_holds`,
          # `reclaim_refuses_a_lock_a_live_process_holds`) are also selected here.
          run_nonzero -p xai-grok-shell --lib reclaim_ -- --nocapture

      # #173: the arm/note_ready ordering invariant from #45 is defended by a
      # comment spanning two crates. These are source-scanning guards: the
      # shell test asserts respawn_stdio inserts into owned_clients BEFORE
      # arming the watcher and never awaits between the arm and Ok(()); the
      # mcp test asserts arm_liveness_watcher itself never awaits after
      # spawn_transport_liveness. Full test names as filters so each selects
      # its test uniquely. Quoted name: an unquoted `#173` truncates at the
      # comment marker.
      - name: 'MCP respawn arm/insert ordering guards (issue #173)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            respawn_stdio_then_inserts_before_arming_and_has_no_post_arm_yields -- --nocapture
          run_nonzero -p xai-grok-mcp --lib \
            arm_liveness_watcher_then_has_no_scheduler_yield_after_spawning -- --nocapture

      # #35: the removed local-workspace bridge surface must fail closed if a
      # client still sends `x.ai/local_workspace`; silent ignore would read as
      # supported and then degrade to NotAttached behavior.
      - name: 'Removed local-workspace meta guard (issue #35)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            local_workspace_removed_meta_rejected_fail_closed_matrix -- --nocapture

      # #188 (one slice): the Codex catalog is fetched instead of hardcoded to a
      # single preset. Every failure here degrades to that preset, which looks
      # exactly like the old behaviour -- so the parser shape and the required
      # query parameter both need a test that fails loudly when they drift.
      # Full unique names so run_nonzero proves each one still runs.
      - name: 'Codex account model catalog (issue #188)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_reads_the_shape_the_endpoint_actually_returns -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_tolerates_other_shapes_and_budgets_the_operative_window -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_context_window_budgets_the_operative_window -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_context_window_keys_prefer_operative_capacity -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_url_carries_the_required_client_version -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_fallback_uses_builtin_preset_when_fetch_is_empty -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            resolve_credentials_refuses_ambient_xai_for_non_first_party_origin -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            openai_codex_path_constructor_ignores_grok_auth_path_env -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            inspect_model_route_line_is_secret_free -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue281_inference_idle_timeout_uses_child_model -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            new_session_keeps_effort_when_supported_menu_is_implicit -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            clone_drop_does_not_cancel_sibling_publication -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            concurrent_uncommitted_clone_drops_remove_private_stage -- --nocapture
          run_nonzero -p xai-grok-workspace --lib \
            session::publication_parent -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            dynamic_enum_choices_disables_by_model_id_not_display_name -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            remap_picking_enum_resets_when_focused_model_disappears -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            remap_picking_enum_keeps_the_focused_model_id_when_display_names_collide -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            action_for_string_commits_catalog_model_id_not_display_name -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            every_dynamic_enum_setting_has_action_for_string_arm -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue244_typed_provider_failure -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue244_headless_typed_provider_failure_omits_acp_internal_error_envelope -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue244_retry_failed_non_retryable_4xx_is_not_labelled_retry_failed -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            codex_catalog_upgrade_migration_is_visible_in_model_picker -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            build_model_items_match_issue17_duplicate_display_name -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            remap_model_picker_selection_keeps_focused_id_when_display_names_collide -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue15_doctor_providers -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            doctor_providers_empty_omits_the_section -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue15_doctor_json_providers_rows_are_secret_free -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue15_provider_facts_from_model_state_mark_unready_without_secrets -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue15_provider_facts_from_model_state_does_not_fabricate_external_trust -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue15_standalone_doctor_collects_provider_rows_from_inspect_routes -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue15_inspect_model_routes_from_toml_covers_keyless_bearer_and_x_api_key -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue15_inspect_model_routes_from_toml_is_empty_when_config_parse_fails -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue15_inspect_model_routes_from_toml_does_not_attempt_live_codex_catalog_fetch -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            issue15_inspect_config_load_does_not_attempt_live_codex_catalog_fetch -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue17_run_commits_focused_catalog_id_when_display_names_collide -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            resolve_by_name_or_id_prefers_catalog_id_when_display_names_collide -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            model_auto_switched_uncatalogued_target_is_unavailable_resident -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            issue49 -- --nocapture
          run_nonzero -p xai-grok-shared --lib \
            simple_mode_alias_loads_legacy_ui_key_issue66 -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            new_from_toml_cfg_accepts_both_readline_mode_and_simple_mode_issue66 -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            load_config_from_toml_keeps_compact_mode_when_both_ui_keys_present -- --nocapture
          run_nonzero -p xai-grok-pager-render --lib \
            simple_mode_alias_prefers_public_key_issue66 -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            accept_fresh_publication_continues_only_for_committed_durability -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_credential_source_must_be_openai_codex_provider -- --nocapture
          # #262: last-good catalogs are account-isolated and degraded state is visible.
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_last_good_cache_is_account_keyed -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_saved_fallback_is_visible_in_acp_metadata -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_degraded_state_survives_description_override -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_builtin_fallback_reports_absent_persisted_default -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            codex_catalog_degraded_state_is_visible_in_model_picker -- --nocapture
          # #306 C-min: Codex-only boot footer + picker readiness presentation
          # (presentation unit tests; fail closed if the exact names vanish).
          run_nonzero -p xai-grok-pager --lib \
            codex_only_boot_footer_uses_authoritative_default_never_unknown -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            codex_model_picker_lists_live_codex_ready_and_grok_unready -- --nocapture
          # #306 remaining gates 3–8 (presentation; fail closed if names vanish)
          run_nonzero -p xai-grok-pager --lib \
            codex_model_picker_selects_every_live_catalog_model -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            codex_effort_picker_obeys_each_model_menu_including_minimal_and_none -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            manual_compaction_renders_activity_and_completion -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            auto_compaction_renders_activity_and_completion -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            codex_web_search_viewer_renders_citations -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            resumed_subagent_badge_and_transcript_are_visible -- --nocapture
          # #303: Codex-only implicit default + reseat (selection policy).
          run_nonzero -p xai-grok-shell --lib \
            codex_only_cold_start_defaults_to_ready_codex -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_only_default_not_bundled_grok_when_codex_ready -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            xai_ambient_still_prefers_first_party_grok -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_ready_reseats_ambient_grok_without_xai_auth -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            byok_unchanged_when_codex_ready -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            auth_scheme_none_unchanged_when_codex_ready -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            deployment_key_keeps_first_party_when_codex_ready -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            third_party_codex_responses_does_not_steal_default -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            blank_xai_env_does_not_pin_grok_when_codex_ready -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            preferred_method_api_key_pin_keeps_first_party_when_codex_ready -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            has_usable_first_party_session_single_snapshot -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            first_party_session_eligibility_refreshable_vs_malformed -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            refreshable_expired_oidc_session_keeps_first_party_when_codex_ready -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            refreshable_expired_external_session_keeps_first_party_when_codex_ready -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            hard_expired_nonrefreshable_session_seats_codex -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_only_from_config_seats_codex_and_sampling_stack -- --nocapture
          # #317: invalid-xAI production probe drain + reseat.
          run_nonzero -p xai-grok-shell --lib \
            initialize_invalid_xai_probe_reseats_implicit_grok_to_ready_codex -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            invalid_env_probe -- --nocapture
          run_nonzero -p xai-grok-test-support --lib \
            read_http_request_headers -- --nocapture
          run_nonzero -p xai-grok-test-support --lib \
            accept_then_drain_survives_nonblocking_listener -- --nocapture
          # #318: EnvGuard users of model-selection env vars must serialize.
          run_nonzero -p xai-grok-test-support --lib \
            env_guard_serializes_concurrent_mutations -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            construction_fallback_is_unusable_when_the_catalog_endpoint_is_external -- --nocapture
          # #245: catalog wire flags that differ from the Sol preset must parse.
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_reads_wire_capabilities_that_differ_from_the_preset -- --nocapture
          # #274: parsed use_responses_lite must reach apply_codex_wire_capabilities.
          run_nonzero -p xai-grok-shell --lib \
            use_responses_lite -- --nocapture
          # #264: catalog 95% is the auto-compact tier below user TOML.
          run_nonzero -p xai-grok-shell --lib \
            effective_context_window_percent -- --nocapture
          # #261: catalog reasoning menus must reach ACP and remain model-specific in the TUI.
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_supported_reasoning_levels_drive_acp_effort_options -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_default_reasoning_level_must_belong_to_supported_levels -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            current_reasoning_effort_rejects_persisted_value_outside_model_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_metadata_override_can_disable_reasoning_effort -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_metadata_override_rejects_default_outside_inherited_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            catalog_refresh_clears_current_effort_removed_from_model_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            config_refresh_clears_current_effort_removed_from_model_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            model_switch_returns_structured_invalid_params_for_effort_outside_model_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            model_switch_accepts_catalog_advertised_ultra_effort -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            unconditional_default_reselection_revalidates_current_effort -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            session_model_effort_validation_uses_requested_model_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            menu_less_reasoning_model_accepts_legacy_minimal_effort -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            rebuild_revalidates_current_reasoning_effort -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_metadata_override_keeps_default_without_inherited_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_metadata_narrower_menu_reconciles_scalar_end_to_end -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_explicit_empty_reasoning_menu_disables_unclaimed_scalar_end_to_end -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            model_state_prefers_session_reasoning_effort_over_global -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            active_none_effort_is_preserved_as_stale_session_mode -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            stale_effort_revalidation_does_not_clear_newer_selection -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            subagent_reasoning_override_obeys_authoritative_model_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            subagent_reasoning_override_uses_legacy_menu_when_catalog_menu_is_absent -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            inherited_subagent_effort_reconciles_after_catalog_refresh -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            resolve_model_override_prepared_carries_authoritative_reasoning_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            session_config_preserves_resident_effort_after_catalog_disables_reasoning -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            menu_less_reasoning_model_accepts_minimal_but_rejects_none_in_effort_command -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            deferred_cli_accepts_minimal_for_menu_less_reasoning_model -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            codex_catalog_effort_options_are_model_specific -- --nocapture
          run_nonzero -p xai-grok-pager --lib \
            pager_model_state_exposes_ultra_in_the_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_preserves_mixed_ultra_menu -- --nocapture
          run_nonzero -p xai-grok-shell --lib \
            codex_catalog_parser_preserves_ultra_only_menu -- --nocapture

      # #36: restore fd bundling only with a public PI caller and contract.
      # The tests are split by build mode: override-path bundle and release
      # auto-download bundle. Filters are written explicitly so #171 can prove
      # each new test runs somewhere.
      - name: 'PI fd glob contract and bundling (issue #36)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }

          run_nonzero -p xai-grok-tools --features pi --lib \
            pi_glob_fd_contract_preserves_public_glob_semantics -- --nocapture
          run_nonzero -p xai-grok-tools --features pi --lib \
            pi_glob_fd_error_falls_back_to_ripgrep -- --nocapture

          GROK_TOOLS_BUNDLE_FD_PATH="$PWD/crates/codegen/xai-grok-tools/tests/fd-override-fixture.sh" \
            run_nonzero -p xai-grok-tools --features pi --lib \
              bundled_fd_override_artifact_extracts_exact_and_executes -- --nocapture

          run_nonzero --release -p xai-grok-tools --features pi --lib \
            bundled_fd_release_artifact_extracts_exact_and_executes -- --nocapture

      # #88: `xai-proto-build` is run by every build script in this workspace and
      # was in no test job at all -- so the code that decides whether protoc is
      # findable, and what gets emitted to cargo's stdout, was never exercised by
      # CI. That stdout is parsed one directive per line, so a value carrying a
      # newline injects further directives; a non-UTF-8 path is exactly the input
      # that reaches it. Quoted name: an unquoted `#88` truncates at the comment
      # marker.
      - name: 'Build-script protoc discovery and rerun-if-changed emission (issue #88)'
        run: |
          run_nonzero() {
            local output
            output="$(mktemp)"
            cargo test "$@" 2>&1 | tee "$output"
            local status="${PIPESTATUS[0]}"
            if [ "$status" -ne 0 ]; then rm -f "$output"; return "$status"; fi
            if grep -qE '^test result: ok\. 0 passed' "$output"; then
              echo "::error::filter matched zero tests: cargo test $*"
              rm -f "$output"; return 1
            fi
            rm -f "$output"
          }
          run_nonzero -p xai-proto-build --lib -- --nocapture
